'use strict';

// Historical import path. The canonical provisioning facade owns typed access
// holds, password-setup state, inactivity cleanup and subscription expiry rules.
module.exports = require('./provisioning');
