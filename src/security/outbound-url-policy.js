'use strict';
const dns=require('dns').promises;
const net=require('net');
const operations=require('../platform/operations-settings');
const CACHE_TTL_MS=30000;
const cache=new Map();
function ipv4(value){const parts=String(value||'').split('.').map(Number);return parts.length===4&&parts.every(n=>Number.isInteger(n)&&n>=0&&n<=255)?parts:null;}
function mappedIpv4(value){const lower=String(value||'').toLowerCase();const m=lower.match(/(?:^|:)ffff:(\d+\.\d+\.\d+\.\d+)$/);return m?m[1]:null;}
function classify(address){const mapped=mappedIpv4(address);if(mapped)return classify(mapped);const version=net.isIP(address);if(version===4){const p=ipv4(address);if(!p)return{hard:true,private:false,reason:'invalid address'};const [a,b]=p;if(a===169&&b===254)return{hard:true,private:true,reason:'link-local / metadata network'};if(a===100&&b>=64&&b<=127)return{hard:true,private:true,reason:'carrier/shared infrastructure range'};if(a===0||a>=224)return{hard:true,private:false,reason:'non-unicast/reserved range'};if(a===198&&(b===18||b===19))return{hard:true,private:false,reason:'benchmark network'};if(a===127)return{hard:false,private:true,reason:'loopback'};if(a===10||(a===172&&b>=16&&b<=31)||(a===192&&b===168))return{hard:false,private:true,reason:'private network'};return{hard:false,private:false,reason:'public'};}if(version===6){const s=String(address).toLowerCase().split('%')[0];if(s==='::'||s==='0:0:0:0:0:0:0:0')return{hard:true,private:false,reason:'unspecified address'};if(s.startsWith('fe8')||s.startsWith('fe9')||s.startsWith('fea')||s.startsWith('feb'))return{hard:true,private:true,reason:'IPv6 link-local network'};if(s.startsWith('ff'))return{hard:true,private:false,reason:'IPv6 multicast network'};if(s==='fd00:ec2::254')return{hard:true,private:true,reason:'cloud metadata endpoint'};if(s==='::1'||s==='0:0:0:0:0:0:0:1')return{hard:false,private:true,reason:'loopback'};const first=parseInt((s.split(':')[0]||'0'),16);if((first&0xfe00)===0xfc00)return{hard:false,private:true,reason:'unique-local network'};return{hard:false,private:false,reason:'public'};}return{hard:true,private:false,reason:'unresolved/non-IP address'};}
async function resolveHost(hostname){const key=String(hostname||'').toLowerCase(),now=Date.now(),prior=cache.get(key);if(prior&&prior.expires>now)return prior.addresses;let addresses;if(net.isIP(key))addresses=[key];else{const rows=await dns.lookup(key,{all:true,verbatim:true});addresses=[...new Set(rows.map(row=>row.address))];}if(!addresses.length)throw new Error('Integration hostname did not resolve to an address.');cache.set(key,{addresses,expires:now+CACHE_TTL_MS});return addresses;}
async function assertSafeIntegrationUrl(raw,{purpose='integration'}={}){let parsed;try{parsed=new URL(String(raw||'').trim())}catch{throw new Error(`Invalid ${purpose} URL.`)}if(!['http:','https:'].includes(parsed.protocol))throw new Error(`${purpose} URL must use http or https.`);if(parsed.username||parsed.password||parsed.hash)throw new Error(`${purpose} URL may not contain credentials or a fragment.`);const cfg=await operations.get().catch(()=>operations.DEFAULTS),trusted=new Set((cfg.outboundTrustedHosts||[]).map(x=>String(x).toLowerCase())),explicit=trusted.has(parsed.hostname.toLowerCase()),addresses=await resolveHost(parsed.hostname);for(const address of addresses){const info=classify(address);if(info.hard)throw new Error(`${purpose} destination ${parsed.hostname} resolved to a blocked ${info.reason}.`);if(info.private&&!cfg.allowPrivateIntegrations&&!explicit)throw new Error(`${purpose} destination resolves to a private address that is not trusted by Operations settings.`);}return{url:parsed.toString(),hostname:parsed.hostname,addresses,private:addresses.some(a=>classify(a).private),explicitlyTrusted:explicit};}
function clearCache(){cache.clear();}
module.exports={classify,resolveHost,assertSafeIntegrationUrl,clearCache};
