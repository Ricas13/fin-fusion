'use strict';
const {query}=require('../db');
const {encryptWithEnv,decryptWithEnv}=require('./purpose-crypto');
const KEY='public_abuse_protection_v1',SECRET_ENV='DATA_ENCRYPTION_KEY',PREFIX='turnstile1';
let cache=null;
function bool(v){return v===true||v==='true'||v==='1'||v==='on'}
function text(v,n=500){return String(v||'').trim().slice(0,n)}
async function load(){const r=await query('SELECT setting_value FROM platform_settings WHERE setting_key=$1',[KEY]),v=r.rows[0]?.setting_value||{};let secret='';if(v.turnstileSecretEncrypted){try{secret=decryptWithEnv(v.turnstileSecretEncrypted,SECRET_ENV,PREFIX)}catch(error){throw new Error(`Turnstile secret could not be decrypted: ${error.message}`)}}return{enabled:Boolean(v.turnstileEnabled),siteKey:text(v.turnstileSiteKey,200),secret,secretConfigured:Boolean(secret),protectRegistration:v.protectRegistration!==false,protectPasswordReset:v.protectPasswordReset!==false}}
async function get(){if(!cache)cache=await load();return{...cache}}
async function reload(){cache=await load();return get()}
async function save(input,actorUserId=null){const existing=(await query('SELECT setting_value FROM platform_settings WHERE setting_key=$1',[KEY])).rows[0]?.setting_value||{};let encrypted=existing.turnstileSecretEncrypted||null;if(bool(input.clearTurnstileSecret))encrypted=null;else if(text(input.turnstileSecret,500))encrypted=encryptWithEnv(text(input.turnstileSecret,500),SECRET_ENV,PREFIX);const value={turnstileEnabled:bool(input.turnstileEnabled),turnstileSiteKey:text(input.turnstileSiteKey,200),turnstileSecretEncrypted:encrypted,protectRegistration:input.protectRegistration!==undefined?bool(input.protectRegistration):true,protectPasswordReset:input.protectPasswordReset!==undefined?bool(input.protectPasswordReset):true};if(value.turnstileEnabled&&(!value.turnstileSiteKey||!value.turnstileSecretEncrypted))throw new Error('Turnstile site key and secret are required when protection is enabled.');await query(`INSERT INTO platform_settings(setting_key,setting_value,updated_by) VALUES($1,$2::jsonb,$3) ON CONFLICT(setting_key) DO UPDATE SET setting_value=EXCLUDED.setting_value,updated_by=EXCLUDED.updated_by,updated_at=NOW()`,[KEY,JSON.stringify(value),actorUserId]);await query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.public_abuse_protection.update','platform_setting',$2,$3::jsonb)`,[actorUserId,KEY,JSON.stringify({enabled:value.turnstileEnabled,protectRegistration:value.protectRegistration,protectPasswordReset:value.protectPasswordReset,secretChanged:Boolean(input.turnstileSecret),secretCleared:bool(input.clearTurnstileSecret)})]);return reload()}
function shouldProtect(cfg,path){return cfg.enabled&&((path==='/account/register'&&cfg.protectRegistration)||(path==='/account/forgot-password'&&cfg.protectPasswordReset))}
async function verifyToken(token,remoteIp){const cfg=await get();if(!cfg.enabled)return{success:true,disabled:true};if(!cfg.secret)throw new Error('Turnstile secret is not configured.');const form=new URLSearchParams({secret:cfg.secret,response:String(token||'')});if(remoteIp)form.set('remoteip',String(remoteIp).slice(0,100));const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),8000);try{const response=await fetch('https://challenges.cloudflare.com/turnstile/v0/siteverify',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:form,signal:controller.signal,redirect:'error'}),body=await response.json().catch(()=>({success:false,'error-codes':['invalid-response']}));if(!response.ok)throw new Error(`Turnstile verification returned HTTP ${response.status}.`);return body}catch(error){if(error.name==='AbortError')throw new Error('Turnstile verification timed out.');throw error}finally{clearTimeout(timer)}}
async function middleware(req,res,next){try{const cfg=await get();if(req.method==='GET'&&['/account/register','/account/forgot-password'].includes(req.path)){res.locals.turnstileEnabled=shouldProtect(cfg,req.path);res.locals.turnstileSiteKey=cfg.siteKey||'';return next()}if(req.method==='POST'&&shouldProtect(cfg,req.path)){const result=await verifyToken(req.body?.['cf-turnstile-response'],req.ip||req.socket?.remoteAddress);if(!result.success){await query(`INSERT INTO audit_log(action,entity_type,entity_id,metadata) VALUES('public.turnstile.reject','public_request',$1,$2::jsonb)`,[req.path,JSON.stringify({errors:result['error-codes']||[]})]).catch(()=>{});res.status(400).setHeader('Cache-Control','no-store');return res.send(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Verification failed</title></head><body style="font-family:system-ui;background:#0b1220;color:#e5e7eb;display:grid;place-items:center;min-height:100vh"><main style="max-width:520px;padding:28px;background:#111827;border-radius:16px"><h1>Verification failed</h1><p>The anti-abuse verification was not accepted. Return to the form and try again.</p><p><a style="color:#7dd3fc" href="${req.path}">Return to form</a></p></main></body></html>`)} }return next()}catch(error){console.error('Public abuse protection failed:',error.message);return res.status(503).send('Verification is temporarily unavailable. Try again shortly.')}}
module.exports={KEY,get,reload,save,verifyToken,shouldProtect,middleware};
