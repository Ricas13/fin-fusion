'use strict';

const crypto=require('crypto');
const bcrypt=require('bcryptjs');
const {query,transaction}=require('./db');
const customers=require('./customers');
const emailOutbox=require('./integrations/email-outbox');
function hashToken(raw){return crypto.createHash('sha256').update(String(raw||''),'utf8').digest('hex')}
function newToken(){return crypto.randomBytes(32).toString('base64url')}
function cleanEmail(value,required=false){const email=String(value||'').trim().toLowerCase();if(!email&&!required)return null;if(!email||!email.includes('@')||email.length>254)throw new Error('Enter a valid email address.');return email}
function cleanUsername(value){const username=String(value||'').trim();if(!/^[A-Za-z0-9._-]{3,40}$/.test(username))throw new Error('Username must be 3–40 characters using letters, numbers, dot, underscore or dash.');return username}
function cleanTtlHours(value){const hours=Number.parseInt(String(value||'168'),10);if(!Number.isInteger(hours)||hours<1||hours>24*30)throw new Error('Claim expiry must be between 1 hour and 30 days.');return hours}
function claimStatus(row,now=new Date()){if(!row)return'missing';if(row.consumed_at)return'claimed';if(row.revoked_at)return'revoked';if(new Date(row.expires_at)<=now)return'expired';return'active'}
async function verificationPrerequisites(email){if(!email)return{required:false,emailLess:true};const runtime=require('./platform/runtime-settings');await runtime.ensureLoaded();if(!runtime.requireEmailVerification())return{required:false};const mail=await require('./integrations/email-settings').status();if(!mail.configured)throw new Error('Email verification is required, but transactional email is not configured.');const ops=require('./platform/operations-settings'),origin=await ops.publicOriginReady();if(!origin.ready)throw new Error(`Email verification is required, but ${origin.reason}`);return{required:true,origin:origin.origin,siteName:runtime.siteName()}}
function verificationMessage(user,prereq,token,expiresAt){const url=`${prereq.origin.replace(/\/$/,'')}/account/verify-email?token=${encodeURIComponent(token)}`;return{type:'email_verification',to:user.email,subject:`Verify your ${prereq.siteName} email address`,text:`Verify the email address for your ${prereq.siteName} customer account: ${url}\n\nThis link expires in 24 hours.`,html:`<p>Verify the email address for your ${prereq.siteName} customer account.</p><p><a href="${url}">Verify email</a></p><p>This link expires in 24 hours.</p>`,dedupeKey:`verify:${user.id}:${expiresAt.toISOString()}`}}
async function queueVerification(user,prereq){if(!prereq.required||!user?.email)return false;const verification=await customers.createAccountToken(user.id,'email_verify',24*60),message=verificationMessage(user,prereq,verification.token,verification.expiresAt);await emailOutbox.enqueue(message);return true}
async function queueVerificationTx(client,user,prereq){if(!prereq.required||!user?.email)return false;if(!client?.query)throw new Error('A database transaction client is required for claim verification.');const token=crypto.randomBytes(32).toString('base64url'),tokenHash=crypto.createHash('sha256').update(token).digest('hex'),expiresAt=new Date(Date.now()+24*60*60000),message=verificationMessage(user,prereq,token,expiresAt);await client.query(`UPDATE account_tokens SET consumed_at=NOW() WHERE user_id=$1 AND token_type='email_verify' AND consumed_at IS NULL`,[user.id]);await client.query(`INSERT INTO account_tokens(user_id,token_type,token_hash,expires_at) VALUES($1,'email_verify',$2,$3)`,[user.id,tokenHash,expiresAt]);const payload={subject:String(message.subject).slice(0,300),text:String(message.text).slice(0,100000),html:String(message.html||'').slice(0,250000)};await client.query(`INSERT INTO notification_outbox(channel,message_type,recipient_email,payload_encrypted,dedupe_key,status,next_attempt_at) VALUES('email',$1,$2,$3,$4,'pending',NOW()) ON CONFLICT(dedupe_key) DO UPDATE SET updated_at=notification_outbox.updated_at`,[message.type,user.email,emailOutbox.encryptPayload(payload),message.dedupeKey]);return true}
async function customerIdentity(customerId){const result=await query(`SELECT c.id,c.user_id,c.display_name,c.email,COALESCE((SELECT ja.jellyfin_username FROM jellyfin_accounts ja WHERE ja.customer_id=c.id ORDER BY ja.is_primary DESC,ja.disabled ASC,ja.created_at ASC LIMIT 1),c.display_name) AS suggested_username,COALESCE((SELECT string_agg(DISTINCT ja.jellyfin_username, ', ' ORDER BY ja.jellyfin_username) FROM jellyfin_accounts ja WHERE ja.customer_id=c.id),'') AS jellyfin_usernames,COALESCE((SELECT string_agg(DISTINCT js.name, ', ' ORDER BY js.name) FROM jellyfin_accounts ja JOIN jellyfin_servers js ON js.id=ja.server_id WHERE ja.customer_id=c.id),'') AS server_names FROM customers c WHERE c.id=$1`,[customerId]);return result.rows[0]||null}
async function createClaim({customerId,emailLock=null,ttlHours=168,actorUserId=null}){const customer=await customerIdentity(customerId);if(!customer)throw new Error('Customer not found.');if(customer.user_id)throw new Error('This customer already has a CAPTAiNFiN portal account.');const lockedEmail=cleanEmail(emailLock,false),hours=cleanTtlHours(ttlHours),token=newToken(),expiresAt=new Date(Date.now()+hours*3600000);const created=await transaction(async client=>{await client.query(`UPDATE customer_account_claims SET revoked_at=COALESCE(revoked_at,NOW()),updated_at=NOW() WHERE customer_id=$1 AND consumed_at IS NULL AND revoked_at IS NULL`,[customerId]);const result=await client.query(`INSERT INTO customer_account_claims(customer_id,token_hash,token_encrypted,email_lock,expires_at,created_by) VALUES($1,$2,NULL,$3,$4,$5) RETURNING *`,[customerId,hashToken(token),lockedEmail,expiresAt,actorUserId]);await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'customer.claim_link.create','customer',$2,$3::jsonb)`,[actorUserId,customerId,JSON.stringify({claimId:result.rows[0].id,emailLock:lockedEmail,expiresAt,tokenStored:false})]);return result.rows[0]});return{claim:created,token,customer}}
async function revokeClaim({claimId,actorUserId=null}){const result=await query(`UPDATE customer_account_claims SET revoked_at=COALESCE(revoked_at,NOW()),updated_at=NOW() WHERE id=$1 AND consumed_at IS NULL RETURNING customer_id`,[claimId]);if(!result.rowCount)throw new Error('Active claim link not found.');await query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'customer.claim_link.revoke','customer',$2,$3::jsonb)`,[actorUserId,result.rows[0].customer_id,JSON.stringify({claimId})]);return true}
async function lookupClaim(rawToken){const result=await query(`SELECT cc.*,c.user_id,c.display_name,c.email AS customer_email,COALESCE((SELECT ja.jellyfin_username FROM jellyfin_accounts ja WHERE ja.customer_id=c.id ORDER BY ja.is_primary DESC,ja.disabled ASC,ja.created_at ASC LIMIT 1),c.display_name) AS suggested_username,COALESCE((SELECT string_agg(DISTINCT ja.jellyfin_username, ', ' ORDER BY ja.jellyfin_username) FROM jellyfin_accounts ja WHERE ja.customer_id=c.id),'') AS jellyfin_usernames,COALESCE((SELECT string_agg(DISTINCT js.name, ', ' ORDER BY js.name) FROM jellyfin_accounts ja JOIN jellyfin_servers js ON js.id=ja.server_id WHERE ja.customer_id=c.id),'') AS server_names FROM customer_account_claims cc JOIN customers c ON c.id=cc.customer_id WHERE cc.token_hash=$1 LIMIT 1`,[hashToken(rawToken)]);if(!result.rowCount)return null;const row=result.rows[0];return{...row,status:row.user_id?'claimed':claimStatus(row)}}
async function redeemClaim({token,username,email=null,password}){username=cleanUsername(username);await customers.validateNewPassword(password);const tokenHash=hashToken(token),pre=await query(`SELECT cc.email_lock FROM customer_account_claims cc WHERE cc.token_hash=$1 AND cc.consumed_at IS NULL AND cc.revoked_at IS NULL AND cc.expires_at>NOW()`,[tokenHash]);if(!pre.rowCount)throw new Error('This claim link is invalid or expired.');const intendedEmail=pre.rows[0].email_lock?cleanEmail(pre.rows[0].email_lock,true):cleanEmail(email,false),verification=await verificationPrerequisites(intendedEmail),passwordHash=await bcrypt.hash(password,12);const created=await transaction(async client=>{const found=await client.query(`SELECT cc.*,c.user_id,c.display_name,c.email AS customer_email FROM customer_account_claims cc JOIN customers c ON c.id=cc.customer_id WHERE cc.token_hash=$1 FOR UPDATE OF cc,c`,[tokenHash]);if(!found.rowCount)throw new Error('This claim link is invalid.');const claim=found.rows[0];if(claim.user_id||claim.consumed_at)throw new Error('This account has already been claimed.');if(claim.revoked_at)throw new Error('This claim link has been revoked.');if(new Date(claim.expires_at)<=new Date())throw new Error('This claim link has expired.');const claimEmail=claim.email_lock?cleanEmail(claim.email_lock,true):cleanEmail(email,false);if(claim.email_lock&&email&&cleanEmail(email,true)!==claimEmail)throw new Error('This claim link is assigned to a different email address.');const duplicate=await client.query(`SELECT username,email FROM app_users WHERE lower(username)=lower($1) OR ($2::text IS NOT NULL AND lower(COALESCE(email,''))=lower($2)) LIMIT 1`,[username,claimEmail]);if(duplicate.rowCount)throw new Error('That username or email is already in use.');const userResult=await client.query(`INSERT INTO app_users(email,username,password_hash,role,active,email_verified_at,password_changed_at) VALUES($1,$2,$3,'customer',TRUE,CASE WHEN $4 THEN NULL WHEN $1::text IS NULL THEN NULL ELSE NOW() END,NOW()) RETURNING id,email,username,role,active,email_verified_at,session_version`,[claimEmail,username,passwordHash,verification.required]),user=userResult.rows[0];const customerResult=await client.query(`UPDATE customers SET user_id=$2,display_name=COALESCE(NULLIF(display_name,''),$3),email=COALESCE($4,email),updated_at=NOW() WHERE id=$1 AND user_id IS NULL RETURNING *`,[claim.customer_id,user.id,username,claimEmail]);if(!customerResult.rowCount)throw new Error('This account has already been claimed.');await client.query(`UPDATE customer_account_claims SET consumed_at=NOW(),claimed_user_id=$2,updated_at=NOW() WHERE id=$1`,[claim.id,user.id]);await client.query(`UPDATE customer_account_claims SET revoked_at=COALESCE(revoked_at,NOW()),updated_at=NOW() WHERE customer_id=$1 AND id<>$2 AND consumed_at IS NULL AND revoked_at IS NULL`,[claim.customer_id,claim.id]);await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'customer.claim.complete','customer',$2,$3::jsonb)`,[user.id,claim.customer_id,JSON.stringify({claimId:claim.id,username,emailSet:Boolean(claimEmail),emailVerificationRequired:verification.required,jellyfinPasswordChanged:false})]);if(verification.required)await queueVerificationTx(client,user,verification);return{user,customer:customerResult.rows[0],claim}});return{...created,emailVerificationQueued:verification.required,verificationRequired:verification.required}}
async function listAdminClaims(){const result=await query(`SELECT c.id AS customer_id,c.display_name,c.email,c.user_id,u.username AS portal_username,u.last_login_at,COALESCE((SELECT string_agg(DISTINCT ja.jellyfin_username, ', ' ORDER BY ja.jellyfin_username) FROM jellyfin_accounts ja WHERE ja.customer_id=c.id),'') AS jellyfin_usernames,COALESCE((SELECT string_agg(DISTINCT js.name, ', ' ORDER BY js.name) FROM jellyfin_accounts ja JOIN jellyfin_servers js ON js.id=ja.server_id WHERE ja.customer_id=c.id),'') AS server_names,cc.id AS claim_id,cc.email_lock,cc.expires_at,cc.consumed_at,cc.revoked_at,cc.created_at AS claim_created_at FROM customers c LEFT JOIN app_users u ON u.id=c.user_id LEFT JOIN LATERAL(SELECT * FROM customer_account_claims x WHERE x.customer_id=c.id ORDER BY x.created_at DESC LIMIT 1)cc ON TRUE WHERE EXISTS(SELECT 1 FROM jellyfin_accounts ja WHERE ja.customer_id=c.id) ORDER BY(c.user_id IS NULL)DESC,c.created_at DESC`);return result.rows.map(row=>({...row,claim_status:row.claim_id?claimStatus(row):'none',raw_token:null}))}
module.exports={hashToken,claimStatus,customerIdentity,createClaim,revokeClaim,lookupClaim,redeemClaim,listAdminClaims,verificationPrerequisites,verificationMessage,queueVerification,queueVerificationTx};