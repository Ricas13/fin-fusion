'use strict';

// Historical import path. The canonical auth service owns explicit step-up,
// runtime session duration and runtime-branded TOTP enrollment behavior.
module.exports = require('./service');
