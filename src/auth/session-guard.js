'use strict';
const auth=require('./service');
const customers=require('../customers');
function destroy(req){return new Promise(resolve=>{if(!req.session)return resolve();req.session.destroy(()=>resolve())})}
async function guardSession(req,res,next){try{if(req.session?.adminId&&!req.session?.authUserId){delete req.session.adminId;delete req.session.userType;return next()}if(req.session?.authUserId&&req.session?.authRole==='admin'){const result=await auth.validateStaffSession(req);if(result.valid)return next();await destroy(req);if(req.path.startsWith('/api/'))return res.status(401).json({success:false,error:'Session expired'});return res.redirect('/login?session=expired')}if(req.session?.customerUserId){const result=await customers.validateCustomerSession(req);if(result.valid)return next();await destroy(req);if(req.path.startsWith('/api/'))return res.status(401).json({success:false,error:'Session expired'});return res.redirect('/account/login?session=expired')}return next()}catch(error){console.error('Session validation failed:',error.message);if(process.env.NODE_ENV==='production'&&(req.session?.authUserId||req.session?.customerUserId)){await destroy(req);return res.status(503).send('Authentication service unavailable')}return next(error)}}
module.exports={guardSession};
