'use strict';

const express=require('express');
const {query}=require('../db');
const csrf=require('../auth/csrf');
const routeRateLimit=require('../security/route-rate-limit');
const runtimeSettings=require('./runtime-settings');
const pool=require('../stremio/source-pool');
const {layout,esc}=require('./admin-html');
const mutate=routeRateLimit.middleware({scope:'admin-stremio-sources',max:30,windowSeconds:300});
function gate(req,res,next){return req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId?next():res.redirect('/login?session=expired');}
function noStore(_req,res,next){res.setHeader('Cache-Control','no-store, private, max-age=0');next();}
function token(req){return `<input type="hidden" name="_csrf" value="${esc(csrf.token(req))}">`;}
function notice(req){return `${req.query.message?`<div class="notice success">${esc(req.query.message)}</div>`:''}${req.query.error?`<div class="notice error">${esc(req.query.error)}</div>`:''}`;}
function date(v){if(!v)return'Never';const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleString('en-GB');}
async function data(){const[sources,settings,managed,recent]=await Promise.all([pool.listSources(),pool.settings(),query(`SELECT id,name,base_url,public_url,enabled,health_status FROM jellyfin_servers ORDER BY enabled DESC,priority,name`),pool.recentRequests(100)]);for(const s of sources)s.accounts=await pool.accounts(s.id);return{sources,settings,managed:managed.rows,recent};}
async function page(req){await runtimeSettings.ensureLoaded();const d=await data(),managedOptions=d.managed.map(s=>`<option value="${esc(s.id)}">${esc(s.name)} · ${esc(s.health_status||'unknown')}</option>`).join('');const sourceRows=d.sources.map(s=>`<section class="section"><div class="sectionHead"><div><h2>${esc(s.name)}</h2><div class="muted">${esc(s.source_type)} · priority ${esc(s.priority)} · weight ${esc(s.weight)} · ${esc(s.account_count)} enabled bridge account(s)</div></div><div><span class="statusPill ${s.enabled?'statusGood':'statusInfo'}">${s.enabled?'Enabled':'Disabled'}</span> <span class="statusPill ${s.index_status==='ready'?'statusGood':s.index_status==='failed'?'statusBad':'statusWarn'}">Index ${esc(s.index_status)}</span></div></div><div class="formGrid"><div class="formGroup"><label>Metadata/API URL</label><div class="codeBlock">${esc(s.base_url)}</div></div><div class="formGroup"><label>Public playback URL</label><div class="codeBlock">${esc(s.public_url)}</div></div><div class="formGroup"><label>Indexed titles</label><strong>${Number(s.item_count||0).toLocaleString('en-GB')}</strong><div class="subText">Last complete ${esc(date(s.last_completed_at))}${s.last_error?` · ${esc(s.last_error)}`:''}</div></div></div><div class="tableWrap"><table class="dataTable"><thead><tr><th>Bridge account</th><th>Jellyfin user</th><th>Weight</th><th>Status</th><th></th></tr></thead><tbody>${s.accounts.length?s.accounts.map(a=>`<tr><td><strong>${esc(a.label)}</strong></td><td>${esc(a.jellyfin_username||a.jellyfin_user_id)}<div class="subText">${esc(a.jellyfin_user_id)}</div></td><td>${esc(a.weight)}</td><td><span class="pill ${a.enabled?'good':''}">${a.enabled?'Enabled':'Off'}</span></td><td class="right"><form method="post" action="/admin/settings/stremio/sources/accounts/${esc(a.id)}/delete" class="inlineForm">${token(req)}<button class="button danger btn-sm">Remove</button></form></td></tr>`).join(''):'<tr><td colspan="5" class="muted">No bridge accounts. Add at least one explicitly authorized Jellyfin user/token.</td></tr>'}</tbody></table></div><form class="formPanel" method="post" action="/admin/settings/stremio/sources/${esc(s.id)}/accounts">${token(req)}<h3>Add bridge account</h3><div class="formGrid"><div class="formGroup"><label>Label</label><input class="input" name="label" required placeholder="Bridge A"></div><div class="formGroup"><label>Jellyfin user ID</label><input class="input" name="jellyfinUserId" required></div><div class="formGroup"><label>Jellyfin username (optional)</label><input class="input" name="jellyfinUsername"></div><div class="formGroup"><label>User access token</label><input class="input" type="password" name="accessToken" required autocomplete="new-password"><div class="inlineHelp">Encrypted at rest with STREMIO_JELLYFIN_TOKEN_KEY.</div></div><div class="formGroup"><label>Selection weight</label><input class="input" type="number" name="weight" min="1" max="10000" value="100"></div></div><input type="hidden" name="enabled" value="1"><button class="button">Add & verify account</button></form><div class="buttonRow"><form method="post" action="/admin/settings/stremio/sources/${esc(s.id)}/index">${token(req)}<button class="button secondary">Queue/rebuild this index</button></form><form method="post" action="/admin/settings/stremio/sources/${esc(s.id)}/delete">${token(req)}<button class="button danger">Delete source</button></form></div></section>`).join('');const body=`${notice(req)}<div class="operatorCallout statusInfo"><strong>Source pool behavior:</strong> CAPTaINFiN may aggregate titles from multiple explicitly authorized Jellyfin sources. Upstream Jellyfin sees the configured bridge account; CAPTaINFiN records the real portal customer/source selection. No upstream activity-concealment or customer impersonation is used.</div><section class="section"><div class="sectionHead"><div><h2>Selection strategy</h2><div class="muted">Choose which indexed source wins when more than one contains the requested title.</div></div></div><form method="post" action="/admin/settings/stremio/sources/settings" class="inlineForm">${token(req)}<select class="input" name="strategy"><option value="weighted_random" ${d.settings.strategy==='weighted_random'?'selected':''}>Weighted random</option><option value="random" ${d.settings.strategy==='random'?'selected':''}>Random</option><option value="priority" ${d.settings.strategy==='priority'?'selected':''}>Priority / fallback</option></select><button class="button">Save</button></form></section><section class="section"><div class="sectionHead"><div><h2>Add source</h2><div class="muted">Managed sources reuse one of your registered Jellyfin servers. External sources require explicit authorization and are checked by the outbound SSRF policy.</div></div></div><div class="formGrid"><form class="formPanel" method="post" action="/admin/settings/stremio/sources">${token(req)}<input type="hidden" name="sourceType" value="managed"><h3>Managed Jellyfin server</h3><div class="formGroup"><label>Name</label><input class="input" name="name" required placeholder="Premium library"></div><div class="formGroup"><label>Server</label><select class="input" name="managedServerId" required><option value="">Choose server…</option>${managedOptions}</select></div><div class="formGrid"><div class="formGroup"><label>Priority</label><input class="input" type="number" name="priority" value="100"></div><div class="formGroup"><label>Weight</label><input class="input" type="number" name="weight" value="100"></div></div><input type="hidden" name="enabled" value="1"><button class="button">Add managed source</button></form><form class="formPanel" method="post" action="/admin/settings/stremio/sources">${token(req)}<input type="hidden" name="sourceType" value="external"><h3>Authorized external Jellyfin</h3><div class="formGroup"><label>Name</label><input class="input" name="name" required placeholder="Partner library"></div><div class="formGroup"><label>API/base URL</label><input class="input" name="baseUrl" required placeholder="https://jellyfin.example.com"></div><div class="formGroup"><label>Public playback URL</label><input class="input" name="publicUrl" required placeholder="https://jellyfin.example.com"></div><div class="formGrid"><div class="formGroup"><label>Priority</label><input class="input" type="number" name="priority" value="100"></div><div class="formGroup"><label>Weight</label><input class="input" type="number" name="weight" value="100"></div></div><input type="hidden" name="enabled" value="1"><button class="button">Add external source</button></form></div></section>${sourceRows||'<div class="empty">No Stremio source-pool sources configured. Existing single-server Stremio delivery remains the fallback.</div>'}<section class="section"><div class="sectionHead"><div><h2>Recent CAPTaINFiN stream attribution</h2><div class="muted">End-customer attribution is kept here even though upstream Jellyfin sees the configured bridge identity.</div></div></div>${d.recent.length?`<div class="tableWrap"><table class="dataTable"><thead><tr><th>Time</th><th>Customer</th><th>Title ID</th><th>Source</th><th>Bridge</th></tr></thead><tbody>${d.recent.map(r=>`<tr><td>${esc(date(r.requested_at))}</td><td><strong>${esc(r.customer_name)}</strong></td><td>${esc(r.video_id)}</td><td>${esc(r.source_name)}</td><td>${esc(r.source_account_label||'—')}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No source-pool stream requests recorded yet.</div>'}</section>`;return layout({siteName:runtimeSettings.siteName(),active:'stremio-settings',title:'Stremio source pool',subtitle:'Authorized multi-Jellyfin aggregation, bridge accounts and customer attribution',body,action:'<a class="button secondary" href="/admin/settings/stremio">Stremio overview</a>'});}
function redirect(res,path,message,error){return res.redirect(`${path}?${error?'error':'message'}=${encodeURIComponent(error||message)}`);}
function createAdminStremioSourcesRouter(){const r=express.Router();r.use('/admin/settings/stremio/sources',gate,noStore);r.get('/admin/settings/stremio/sources',async(req,res,next)=>{try{return res.send(await page(req));}catch(e){next(e)}});r.post('/admin/settings/stremio/sources/settings',mutate,async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{await pool.saveSettings(req.body,req.session.authUserId);return redirect(res,'/admin/settings/stremio/sources','Source selection strategy saved.');}catch(e){return redirect(res,'/admin/settings/stremio/sources',null,e.message)}});r.post('/admin/settings/stremio/sources',mutate,async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const s=await pool.saveSource(req.body,req.session.authUserId);return redirect(res,'/admin/settings/stremio/sources',`Source ${s.name} added. Add a bridge account next.`);}catch(e){return redirect(res,'/admin/settings/stremio/sources',null,e.message)}});r.post('/admin/settings/stremio/sources/:id/accounts',mutate,async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{await pool.saveAccount(req.params.id,req.body,req.session.authUserId);await query(`INSERT INTO automation_job_state(job_key,enabled,interval_seconds,next_run_at,force_run_requested) VALUES('stremio_media_index',TRUE,21600,NOW(),TRUE) ON CONFLICT(job_key) DO UPDATE SET enabled=TRUE,next_run_at=NOW(),force_run_requested=TRUE,updated_at=NOW()`);return redirect(res,'/admin/settings/stremio/sources','Bridge account verified; media index refresh queued.');}catch(e){return redirect(res,'/admin/settings/stremio/sources',null,e.message)}});r.post('/admin/settings/stremio/sources/accounts/:id/delete',mutate,async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{await pool.deleteAccount(req.params.id,req.session.authUserId);return redirect(res,'/admin/settings/stremio/sources','Bridge account removed.');}catch(e){return redirect(res,'/admin/settings/stremio/sources',null,e.message)}});r.post('/admin/settings/stremio/sources/:id/delete',mutate,async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{await pool.deleteSource(req.params.id,req.session.authUserId);return redirect(res,'/admin/settings/stremio/sources','Source removed.');}catch(e){return redirect(res,'/admin/settings/stremio/sources',null,e.message)}});r.post('/admin/settings/stremio/sources/:id/index',mutate,async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{await query(`INSERT INTO automation_job_state(job_key,enabled,interval_seconds,next_run_at,force_run_requested) VALUES('stremio_media_index',TRUE,21600,NOW(),TRUE) ON CONFLICT(job_key) DO UPDATE SET enabled=TRUE,next_run_at=NOW(),force_run_requested=TRUE,updated_at=NOW()`);return redirect(res,'/admin/settings/stremio/sources','Media-index refresh queued.');}catch(e){return redirect(res,'/admin/settings/stremio/sources',null,e.message)}});return r;}
module.exports={createAdminStremioSourcesRouter,page,data};
