'use strict';

const express=require('express');
const {query,transaction}=require('../db');
const csrf=require('../auth/csrf');
const runtimeSettings=require('./runtime-settings');
const capacity=require('../entitlements/plan-capacity');
const {esc,layout}=require('./admin-html');

function gate(req,res,next){return req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId?next():res.redirect('/login?session=expired');}
function noStore(_req,res,next){res.setHeader('Cache-Control','no-store, private, max-age=0');res.setHeader('Pragma','no-cache');next();}
function notice(req){return `${req.query.message?`<div class="notice success">${esc(req.query.message)}</div>`:''}${req.query.error?`<div class="notice error">${esc(req.query.error)}</div>`:''}`;}
async function plan(id){return(await query('SELECT id,code,name,capacity_limit,service_type FROM plans WHERE id=$1 AND archived_at IS NULL',[id])).rows[0]||null;}
async function page(req){await runtimeSettings.ensureLoaded();const p=await plan(req.params.id);if(!p)return null;const usage=await capacity.usage(p.id),remaining=usage.limit==null?null:Math.max(0,usage.limit-usage.used),body=`${notice(req)}<div class="metrics"><div class="metric"><div class="metricLabel">Used slots</div><div class="metricValue">${esc(usage.used)}</div></div><div class="metric"><div class="metricLabel">Availability limit</div><div class="metricValue">${usage.limit==null?'—':esc(usage.limit)}</div></div><div class="metric"><div class="metricLabel">Available now</div><div class="metricValue ${remaining===0?'statusBad':'statusGood'}">${remaining==null?'Not configured':esc(remaining)}</div></div></div><section class="section"><div class="sectionHead"><div><h2>Storefront availability</h2><div class="muted">Capacity controls new acquisition only. Existing active/paid-through entitlements remain valid if you lower the limit below current use.</div></div></div><form class="formPanel" method="post" action="/admin/plans/${esc(p.id)}/inventory"><input type="hidden" name="_csrf" value="${esc(csrf.token(req))}"><div class="formGroup narrow"><label>Maximum concurrent plan slots</label><input class="input" type="number" name="capacityLimit" min="0" max="1000000" required value="${esc(p.capacity_limit??'0')}"><div class="inlineHelp">Set this to 0 to close new acquisition while keeping the plan configured. Raise it when you are ready to accept customers.</div></div><button class="button">Save availability</button></form></section><div class="operatorCallout"><strong>Contract safety:</strong> changing availability never cancels subscriptions, removes Jellyfin access or alters the terms already captured on an existing entitlement.</div>`;return layout({siteName:runtimeSettings.siteName(),active:'plans',title:`${p.name} · Availability`,subtitle:'Live slot availability and acquisition capacity',body});}
function createAdminPlanInventoryRouter(){const r=express.Router();r.use('/admin/plans/:id/inventory',gate,noStore);r.get('/admin/plans/:id/inventory',async(req,res,next)=>{try{const html=await page(req);return html?res.send(html):res.status(404).send('Plan not found');}catch(error){next(error)}});r.post('/admin/plans/:id/inventory',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const raw=String(req.body.capacityLimit||'').trim(),n=Number.parseInt(raw,10);if(!Number.isInteger(n)||String(n)!==raw||n<0||n>1000000)throw new Error('Availability limit must be a whole number from 0 to 1,000,000.');await transaction(async client=>{const updated=await client.query('UPDATE plans SET capacity_limit=$2,updated_at=NOW() WHERE id=$1 AND archived_at IS NULL RETURNING code,name',[req.params.id,n]);if(!updated.rowCount)throw new Error('Plan not found.');await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.plan.inventory.update','plan',$2,$3::jsonb)`,[req.session.authUserId,req.params.id,JSON.stringify({capacityLimit:n})]);});return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/inventory?message=${encodeURIComponent(n===0?'Plan availability closed at 0 slots.':'Plan availability saved.')}`);}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/inventory?error=${encodeURIComponent(error.message)}`)}});return r;}
module.exports={createAdminPlanInventoryRouter,page};
