'use strict';

const express=require('express');
const {query,transaction}=require('../db');
const csrf=require('../auth/csrf');
const runtimeSettings=require('./runtime-settings');
const planPricing=require('../payments/plan-pricing');
const streamVariants=require('../payments/stream-variants');
const verifier=require('../payments/provider-mapping-verification');
const routeRateLimit=require('../security/route-rate-limit');
const {esc,layout}=require('./admin-html');
const {planById,planSubnav}=require('./admin-plans');

const mutationLimit=routeRateLimit.middleware({scope:'admin-plan-stream-variants',max:40,windowSeconds:3600});
function gate(req,res,next){if(req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId)return next();return res.redirect('/login?session=expired');}
function noStore(_req,res,next){res.setHeader('Cache-Control','no-store, private, max-age=0');res.setHeader('Pragma','no-cache');next();}
function csrfInput(req){return `<input type="hidden" name="_csrf" value="${esc(csrf.token(req))}">`;}
function checked(value){return value==='on'||value==='1'||value===true;}
function cents(value,currency){const n=Number(value);if(!Number.isFinite(n)||n<=0||n>1000000)throw new Error(`Enter a valid ${currency} price above zero.`);return Math.round(n*100);}
function external(value){return String(value||'').trim().slice(0,200);}
function eligible(plan){return streamVariants.eligible(plan)&&String(plan.server_class||'premium')==='premium';}
function choices(plan){const base=Math.max(1,Number(plan.streams||1));return [base+1,base+2,base+3].filter(n=>n<=50);}
function mappingRow(variant,provider,mode){return variant?.mappings?.get(`${provider}:${mode}`)||null;}
function verification(row){if(!row)return'';const status=row.verification_status||'unverified',kind=status==='verified'||status==='not_required'?'good':status==='drift'||status==='error'?'bad':'warn';return `<div class="securityNote standalone"><span class="pill ${kind}">${esc(status)}</span>${row.verified_at?` · ${esc(new Date(row.verified_at).toLocaleString('en-GB'))}`:''}${row.verification_error?`<div class="subText">${esc(row.verification_error)}</div>`:''}</div>`;}
function mappingControl(streams,provider,mode,row,{externalRequired=false,label}){const key=`s${streams}_${provider}_${mode}`;return `<div class="serverCard"><div class="serverTop"><strong>${esc(label)}</strong><span class="pill ${row?.mapping_active?'good':''}">${row?.mapping_active?'Enabled':'Off'}</span></div><label class="toggleRow"><input type="checkbox" name="${key}_enabled" ${row?.mapping_active?'checked':''}><span>Offer this payment option</span></label>${externalRequired?`<div class="formGroup"><label>${provider==='stripe'?'Stripe Price ID':'PayPal Billing Plan ID'}</label><input class="input" name="${key}_external_id" value="${esc(row?.external_id||'')}" placeholder="${provider==='stripe'?'price_...':'P-...'}"></div>`:`<div class="securityNote standalone">One-time PayPal uses the selected CAPTAiNFiN amount directly.</div>`}${verification(row)}</div>`;}
function variantCard(req,plan,currency,streams,variant){const enabled=Boolean(variant?.active),minor=Number(variant?.price_minor||0);return `<section class="section"><div class="sectionHead"><div><h2>${streams} concurrent streams</h2><div class="muted">Dedicated immutable commercial variant of ${esc(plan.name)}.</div></div><span class="pill ${enabled?'good':''}">${enabled?'Available':'Not offered'}</span></div><div class="formPanel"><label class="toggleRow"><input type="checkbox" name="s${streams}_active" ${enabled?'checked':''}><span>Offer ${streams} streams at checkout</span></label><div class="formGroup narrow"><label>Price (${esc(currency)})</label><input class="input" type="number" step="0.01" min="0.01" name="s${streams}_price" value="${minor>0?(minor/100).toFixed(2):''}" placeholder="0.00"></div><h3>Stripe</h3><div class="serverGrid">${mappingControl(streams,'stripe','payment',mappingRow(variant,'stripe','payment'),{externalRequired:true,label:'One-time payment'})}${mappingControl(streams,'stripe','subscription',mappingRow(variant,'stripe','subscription'),{externalRequired:true,label:'Recurring subscription'})}</div><h3 style="margin-top:22px">PayPal</h3><div class="serverGrid">${mappingControl(streams,'paypal','payment',mappingRow(variant,'paypal','payment'),{externalRequired:false,label:'One-time payment'})}${mappingControl(streams,'paypal','subscription',mappingRow(variant,'paypal','subscription'),{externalRequired:true,label:'Recurring subscription'})}</div></div></section>`;}

async function summary(plan,currency){if(!eligible(plan))return'';const variants=await streamVariants.adminState(plan.id,currency),active=variants.filter(v=>v.active).length;return `<section class="section"><div class="sectionHead"><div><h2>Stream choices</h2><div class="muted">Keep one customer-facing plan while offering dedicated 3/4/5/6-style commercial variants behind the selector.</div></div><span class="pill ${active?'good':''}">${active?`${active} extra option${active===1?'':'s'}`:'Base only'}</span></div><div class="securityNote standalone"><strong>${esc(plan.streams||1)} streams are included in the base price.</strong><div class="subText">Extra stream choices use their own Stripe/PayPal recurring price IDs, so each subscription renews at exactly the amount selected at checkout. Existing snapshots remain grandfathered if you change the catalogue later.</div></div><a class="button secondary" href="/admin/plans/${esc(plan.id)}/stream-variants">Configure stream choices</a></section>`;}

async function page(req,plan){await runtimeSettings.ensureLoaded();if(!eligible(plan))throw new Error('Stream choices are available only for paid Premium Jellyfin plans.');const currency=await planPricing.platformDefaultCurrency(),state=await streamVariants.adminState(plan.id,currency),map=new Map(state.map(v=>[Number(v.streams),v])),opts=choices(plan);const body=`${req.query.message?`<div class="notice success">${esc(req.query.message)}</div>`:''}${req.query.error?`<div class="notice error">${esc(req.query.error)}</div>`:''}${planSubnav(plan.id,'commerce',plan.service_type)}<section class="section"><div class="sectionHead"><div><h2>Stream choices</h2><div class="muted">The base plan remains ${esc(plan.streams||1)} streams. Configure up to three higher stream limits without creating duplicate customer-facing plans.</div></div><a class="button secondary" href="/admin/plans/${esc(plan.id)}/commerce">Back to Pricing</a></div><div class="securityNote standalone"><strong>How it works:</strong> customers see one plan card and choose their simultaneous stream limit. Each choice has its own amount/provider price. CAPTAiNFiN snapshots the selected streams, price and provider ID into the subscription contract.</div></section><form method="post" action="/admin/plans/${esc(plan.id)}/stream-variants">${csrfInput(req)}${opts.map(streams=>variantCard(req,plan,currency,streams,map.get(streams))).join('')}<div class="buttonRow"><button class="button">Verify & save stream choices</button><a class="button secondary" href="/admin/plans/${esc(plan.id)}/commerce">Cancel</a></div></form>`;return layout({siteName:runtimeSettings.siteName(),active:'plans',title:plan.name,subtitle:`Stream choices · ${currency}`,body});}

async function verify(plan,priceMinor,currency,provider,mode,enabled,externalId){if(!enabled)return null;if((provider==='stripe'||mode==='subscription')&&!externalId)throw new Error(`${provider==='stripe'?'Stripe':'PayPal'} ${mode==='payment'?'one-time':'subscription'} ID is required.`);if(provider==='paypal'&&mode==='payment')return{verificationStatus:'not_required',verifiedAt:new Date(),verificationError:null,remoteAmountMinor:priceMinor,remoteCurrency:currency,remoteInterval:null,remoteActive:true};const result=await verifier.verify(provider,externalId,{priceMinor,currency,checkoutMode:mode==='payment'?'one_time':'subscription',billingInterval:plan.billing_interval});if(result.issues.length)throw new Error(`${provider==='stripe'?'Stripe':'PayPal'} ${mode==='payment'?'one-time':'subscription'} for stream choice does not match ${currency} ${(priceMinor/100).toFixed(2)}: ${result.issues.join('; ')}`);return verifier.fields(result);}
async function saveMapping(client,variantId,provider,mode,enabled,externalId,v){if(!enabled){await client.query('DELETE FROM plan_stream_variant_provider_prices WHERE stream_variant_id=$1 AND provider=$2 AND checkout_mode=$3',[variantId,provider,mode]);return;}await client.query(`INSERT INTO plan_stream_variant_provider_prices(stream_variant_id,provider,checkout_mode,external_id,active,verified_at,verification_status,verification_error,remote_amount_minor,remote_currency,remote_interval,remote_active) VALUES($1,$2,$3,$4,TRUE,$5,$6,$7,$8,$9,$10,$11) ON CONFLICT(stream_variant_id,provider,checkout_mode) DO UPDATE SET external_id=EXCLUDED.external_id,active=TRUE,verified_at=EXCLUDED.verified_at,verification_status=EXCLUDED.verification_status,verification_error=EXCLUDED.verification_error,remote_amount_minor=EXCLUDED.remote_amount_minor,remote_currency=EXCLUDED.remote_currency,remote_interval=EXCLUDED.remote_interval,remote_active=EXCLUDED.remote_active,updated_at=NOW()`,[variantId,provider,mode,externalId||null,v.verifiedAt,v.verificationStatus,v.verificationError,v.remoteAmountMinor,v.remoteCurrency,v.remoteInterval,v.remoteActive]);}

function createAdminPlanStreamVariantsRouter(){const router=express.Router();router.use('/admin/plans',gate,noStore);router.get('/admin/plans/:id/stream-variants',async(req,res,next)=>{try{const plan=await planById(req.params.id);if(!plan)return res.status(404).send('Plan not found');return res.send(await page(req,plan));}catch(error){return next(error);}});router.post('/admin/plans/:id/stream-variants',mutationLimit,async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const plan=await planById(req.params.id);if(!plan)throw new Error('Plan not found');if(!eligible(plan))throw new Error('Stream choices are available only for paid Premium Jellyfin plans.');const currency=await planPricing.platformDefaultCurrency(),specs=[];for(const streams of choices(plan)){const active=checked(req.body[`s${streams}_active`]);let priceMinor=0;if(active)priceMinor=cents(req.body[`s${streams}_price`],currency);const mappings=[];for(const[provider,mode]of[['stripe','payment'],['stripe','subscription'],['paypal','payment'],['paypal','subscription']]){const key=`s${streams}_${provider}_${mode}`,enabled=active&&checked(req.body[`${key}_enabled`]),externalId=external(req.body[`${key}_external_id`]),verification=await verify(plan,priceMinor,currency,provider,mode,enabled,externalId);mappings.push({provider,mode,enabled,externalId,verification});}specs.push({streams,active,priceMinor,mappings});}await transaction(async client=>{for(const spec of specs){const row=await client.query(`INSERT INTO plan_stream_variants(plan_id,streams,currency,price_minor,active) VALUES($1,$2,$3,$4,$5) ON CONFLICT(plan_id,streams,currency) DO UPDATE SET price_minor=EXCLUDED.price_minor,active=EXCLUDED.active,updated_at=NOW() RETURNING id`,[plan.id,spec.streams,currency,spec.priceMinor,spec.active]),variantId=row.rows[0].id;for(const mapping of spec.mappings)await saveMapping(client,variantId,mapping.provider,mapping.mode,mapping.enabled,mapping.externalId,mapping.verification);if(!spec.active)await client.query('UPDATE plan_stream_variant_provider_prices SET active=FALSE,updated_at=NOW() WHERE stream_variant_id=$1',[variantId]);}await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.plan.stream_variants','plan',$2,$3::jsonb)`,[req.session.authUserId,plan.id,JSON.stringify({currency,baseStreams:Number(plan.streams||1),choices:specs.map(s=>({streams:s.streams,active:s.active,priceMinor:s.priceMinor}))})]);});return res.redirect(`/admin/plans/${encodeURIComponent(plan.id)}/stream-variants?message=`+encodeURIComponent('Stream choices verified and saved.'));}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/stream-variants?error=`+encodeURIComponent(error.message||'Stream choices could not be saved.'));}});return router;}

module.exports={createAdminPlanStreamVariantsRouter,summary,page,eligible,choices};
