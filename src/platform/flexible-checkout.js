'use strict';
const express=require('express');
const customers=require('../customers');
const stripe=require('../payments/stripe');
const paypal=require('../payments/paypal');
const plisio=require('../payments/plisio');
const providerPricing=require('../payments/provider-plan-pricing');
const planPricing=require('../payments/plan-pricing');
const providerSettings=require('../payments/provider-settings');
const intents=require('../payments/checkout-intents');
const discounts=require('../payments/discounts');
const planChange=require('../payments/customer-plan-change');
const serviceScope=require('../entitlements/service-scope');
const commerce=require('../payments/commerce-control');
const serviceCreditReservations=require('../payments/service-credit-reservations');
const {query}=require('../db');
const csrf=require('../auth/csrf');
const runtimeSettings=require('./runtime-settings');
const operations=require('./operations-settings');
const routeRateLimit=require('../security/route-rate-limit');
const publicError=require('./public-error');
const checkoutStartLimit=routeRateLimit.middleware({scope:'customer-checkout-start',max:20,windowSeconds:3600});
const CHECKOUT_SAFE=[
 'Invalid or expired security token','Plan is required',
 /^This plan is not configured for .+ in .+$/,/^That payment type is not available for this plan in .+$/,
 /^(Stripe|PayPal|Plisio) checkout is temporarily unavailable until its payment callback is configured\.$/,
 'PayPal discount codes currently apply only to one-time payments',
 'Service credit cannot be combined with a recurring PayPal subscription. Use Stripe, a one-time PayPal option, or full service credit.',
 'Service credit cannot be mixed with a Plisio crypto payment. Use the full service-credit option when your balance covers the plan, or pay the Plisio amount without service credit.',
 /^Mixed service-credit checkout is for activating a new .+ service\. End the current overlapping service before applying credit to a new provider checkout\.$/,
 /^You already have recurring .+ access\. Manage or cancel that service before starting another recurring subscription\.$/,
 'That discount code is not valid or has expired','That discount code does not apply to this plan','That discount code has reached its redemption limit',
 'You have already used that discount code','You have already used or reserved that discount code',
 'Checkout intent was not found for this discount reservation','Discount reservation customer does not match the checkout intent',
 'Discounts can only be reserved for an open checkout intent','Checkout intent has already expired',
 'Checkout intent is not available for service credit.',
 'Checkout intent not found.','Checkout intent has expired or was already used.','Checkout state verification failed.',
 'Provider checkout does not match the local checkout intent.','Checkout intent belongs to a different billing scope.',
 'Checkout intent belongs to a different provider.','Checkout intent belongs to a different account.','Invalid checkout completion state.',
 'A checkout is already in progress. Finish or cancel it before starting another one.'
];
function checkoutErrorRedirect(res,error,context,fallback){const{message}=publicError.present(error,{context,fallback,safe:CHECKOUT_SAFE});return res.redirect('/account?error='+encodeURIComponent(message))}
function requireCustomer(req,res,next){return req.session?.customerId&&req.session?.customerUserId?next():res.redirect('/account/login?next='+encodeURIComponent(req.originalUrl||'/account'))}
function providerLabel(provider){return({stripe:'Stripe',paypal:'PayPal',plisio:'Plisio'})[provider]||'Payment provider'}
function priceLabel(plan){try{return new Intl.NumberFormat('en-GB',{style:'currency',currency:plan.currency||'GBP',currencyDisplay:'narrowSymbol'}).format(Number(plan.price_minor||0)/100)}catch{return `${plan.currency||'GBP'} ${(Number(plan.price_minor||0)/100).toFixed(2)}`}}
function requestedAccessQuantity(req){const n=Number(req.body.accessQuantity);return Number.isInteger(n)&&n>0?n:null;}
function choiceQuantity(plan){return Number(plan?.access_quantity||plan?.quantity||(plan?.variant_kind==='households'?plan?.stremio_household_network_limit:plan?.streams)||1);}
async function requestedCurrency(_req){return planPricing.platformDefaultCurrency();}
async function checkoutReadiness(){const statuses=await providerSettings.checkoutStatus();return Object.fromEntries(Object.entries(statuses).map(([provider,status])=>[provider,Boolean(status?.checkoutReady)]));}
async function assertProviderCheckoutReady(provider){const status=await providerSettings.status(provider);if(!status.checkoutReady)throw new Error(`${providerLabel(provider)} checkout is temporarily unavailable until its payment callback is configured.`);return status;}
async function chooseOrResolve(req,res,provider){if(!csrf.verify(req))throw new Error('Invalid or expired security token');await commerce.assertOpen();const planCode=String(req.body.planCode||'').trim();if(!planCode)throw new Error('Plan is required');const currency=await requestedCurrency(req),quantity=requestedAccessQuantity(req),options=await providerPricing.getProviderOptions(planCode,provider,currency,quantity);if(!options.length)throw new Error(`This plan is not configured for ${providerLabel(provider)} in ${currency}`);const requested=['payment','subscription'].includes(req.body.checkoutMode)?req.body.checkoutMode:null;if(requested){const match=options.find(option=>option.checkout_mode===requested);if(!match)throw new Error(`That payment type is not available for this plan in ${currency}`);if(provider==='paypal'&&requested==='subscription'&&req.body.discountCode)throw new Error('PayPal discount codes currently apply only to one-time payments');return{mode:requested,planCode,currency,options,plan:match,accessQuantity:choiceQuantity(match),accessVariantKind:match.variant_kind||null}}if(options.length===1)return{mode:options[0].checkout_mode,planCode,currency,options,plan:options[0],accessQuantity:choiceQuantity(options[0]),accessVariantKind:options[0].variant_kind||null};await runtimeSettings.ensureLoaded();const first=options[0];res.render('customer/payment-choice',{siteName:runtimeSettings.siteName(),provider,planCode,planName:first.name,priceLabel:priceLabel(first),options,currency,accessQuantity:choiceQuantity(first),accessVariantKind:first.variant_kind||null,discountCode:String(req.body.discountCode||'').trim().slice(0,40),csrfToken:csrf.token(req)});return null}
async function stateUrl(req,path,intent){const url=new URL(await operations.absoluteUrl(req,path));url.searchParams.set('checkout_intent',intent.id);url.searchParams.set('checkout_state',intent.nonce);return url.toString()}
async function stripeSuccessUrl(req,intent){return `${await stateUrl(req,'/account/stripe/return',intent)}&session_id={CHECKOUT_SESSION_ID}`;}
function commercialSnapshot(choice,provider,discount=null){const p=choice.plan,variantKind=choice.accessVariantKind||p.variant_kind||null,quantity=choice.accessQuantity||choiceQuantity(p);return{kind:'direct_plan',planId:p.id,planPriceId:p.plan_price_id||null,planCode:p.code,planName:p.name,accessVariantId:p.access_variant_id||null,accessVariantKind:variantKind,accessQuantity:quantity,priceMinor:Number(p.price_minor||0),currency:String(p.currency||'').toUpperCase(),billingInterval:p.billing_interval,durationDays:Number(p.duration_days||30),streams:variantKind==='streams'?quantity:Number(p.streams||1),stremioHouseholdNetworkLimit:variantKind==='households'?quantity:Number(p.stremio_household_network_limit||1),allowDownloads:Boolean(p.allow_downloads),allowVideoTranscoding:Boolean(p.allow_video_transcoding),allowAudioTranscoding:Boolean(p.allow_audio_transcoding),allowLiveTv:Boolean(p.allow_live_tv),allowLiveTvManagement:Boolean(p.allow_live_tv_management),serverClass:p.server_class,requestMovieQuotaLimit:p.request_movie_quota_limit==null?null:Number(p.request_movie_quota_limit),requestMovieQuotaDays:p.request_movie_quota_days==null?null:Number(p.request_movie_quota_days),requestTvQuotaLimit:p.request_tv_quota_limit==null?null:Number(p.request_tv_quota_limit),requestTvQuotaDays:p.request_tv_quota_days==null?null:Number(p.request_tv_quota_days),provider,checkoutMode:choice.mode,providerMappingId:p.external_id||null,providerMappingRecordId:p.provider_mapping_id||null,discountCodeId:discount?.discount?.id||null,discountCode:discount?.discount?.code||null,discountedMinor:discount?.discountedMinor??Number(p.price_minor||0),discountReservationId:discount?.reservation?.id||null};}
async function livePrimaryRows(customerId){return(await query(`SELECT s.id,s.source,s.provider_subscription_id,s.service_type_snapshot,p.service_type,p.name,p.is_free_tier FROM subscriptions s JOIN plans p ON p.id=s.plan_id WHERE s.customer_id=$1 AND COALESCE(p.is_addon,FALSE)=FALSE AND s.superseded_by IS NULL AND s.starts_at<=NOW() AND s.status IN('active','trialing','past_due','paused') AND s.current_period_end>NOW()`,[customerId])).rows;}
async function overlappingRecurring(customerId,target){const rows=await livePrimaryRows(customerId);return rows.filter(row=>serviceScope.overlaps(row,target)&&((row.source==='stripe'&&/^sub_/i.test(String(row.provider_subscription_id||'')))||(row.source==='paypal'&&/^I-/i.test(String(row.provider_subscription_id||'')))));}
async function begin(req,res,provider){await assertProviderCheckoutReady(provider);const choice=await chooseOrResolve(req,res,provider);if(!choice||res.headersSent)return null;const wantsCredit=['on','true','1','yes'].includes(String(req.body.applyServiceCredit||'').toLowerCase());if(wantsCredit&&provider==='paypal'&&choice.mode==='subscription')throw new Error('Service credit cannot be combined with a recurring PayPal subscription. Use Stripe, a one-time PayPal option, or full service credit.');if(wantsCredit&&provider==='plisio')throw new Error('Service credit cannot be mixed with a Plisio crypto payment. Use the full service-credit option when your balance covers the plan, or pay the Plisio amount without service credit.');if(wantsCredit){const conflict=(await livePrimaryRows(req.session.customerId)).find(row=>serviceScope.overlaps(row,choice.plan)&&!serviceScope.isFreeTier(row));if(conflict)throw new Error(`Mixed service-credit checkout is for activating a new ${serviceScope.label(choice.plan)} service. End the current overlapping service before applying credit to a new provider checkout.`);}if(choice.mode==='subscription'&&!wantsCredit){const recurring=await overlappingRecurring(req.session.customerId,choice.plan);if(recurring.length){const jellyfinCurrent=await query(`SELECT service_type_snapshot,service_type FROM effective_customer_entitlements WHERE customer_id=$1 LIMIT 1`,[req.session.customerId]);if(!jellyfinCurrent.rowCount||!serviceScope.overlaps(jellyfinCurrent.rows[0],choice.plan))throw new Error(`You already have recurring ${serviceScope.label(choice.plan)} access. Manage or cancel that service before starting another recurring subscription.`);const changed=await planChange.requestChange({customerId:req.session.customerId,targetPlanCode:choice.planCode,targetCurrency:choice.currency,timing:req.body.changeTiming||'auto',actorUserId:req.session.customerUserId});if(changed.handled)return{internal:true,message:changed.message};throw new Error(`You already have recurring ${serviceScope.label(choice.plan)} access. Manage or cancel that service before starting another recurring subscription.`);}}const checkoutTtlMinutes=intents.providerMaxTtl(provider);const intent=await intents.createIntent({scope:'customer',customerId:req.session.customerId,planId:choice.plan.id,planPriceId:choice.plan.plan_price_id,provider,checkoutMode:choice.mode,ttlMinutes:checkoutTtlMinutes,commercialSnapshot:commercialSnapshot(choice,provider)});try{let reservation=null;if(req.body.discountCode)reservation=await discounts.reserveForIntent({code:req.body.discountCode,planCode:choice.planCode,customerId:req.session.customerId,checkoutIntentId:intent.id,baseMinor:Number(choice.plan.price_minor||0),ttlMinutes:checkoutTtlMinutes});let snapshot=commercialSnapshot(choice,provider,reservation),grossDue=Number((snapshot.discountedMinor??snapshot.priceMinor)||0),serviceCreditMinor=0;if(wantsCredit&&grossDue>50){const creditTtl=provider==='paypal'?7*60*60*1000:70*60*1000;const credit=await serviceCreditReservations.reserveForIntent({customerId:req.session.customerId,checkoutIntentId:intent.id,currency:choice.currency,maxAmountMinor:grossDue-50,expiresAt:new Date(Date.now()+creditTtl)});serviceCreditMinor=Number(credit.amountMinor||0);}const providerDue=Math.max(0,grossDue-serviceCreditMinor);snapshot={...snapshot,grossDiscountedMinor:grossDue,serviceCreditMinor,serviceCreditCurrency:serviceCreditMinor?choice.currency:null,discountedMinor:providerDue};const updated=await query(`UPDATE billing_checkout_intents SET commercial_snapshot=$2::jsonb,updated_at=NOW() WHERE id=$1 RETURNING commercial_snapshot,expires_at`,[intent.id,JSON.stringify(snapshot)]);intent.commercial_snapshot=updated.rows[0]?.commercial_snapshot||snapshot;intent.expires_at=updated.rows[0]?.expires_at||intent.expires_at;if(provider==='stripe'){const portal=await customers.getCustomerPortal(req.session.customerId);const checkout=await stripe.createCheckout({customerId:req.session.customerId,planCode:choice.planCode,checkoutMode:choice.mode,email:portal?.customer?.login_email||portal?.customer?.email,discountCode:req.body.discountCode||null,successUrl:await stripeSuccessUrl(req,intent),cancelUrl:await stateUrl(req,'/account/checkout/cancel',intent),idempotencyKey:intent.id,commercialSnapshot:intent.commercial_snapshot,resolvedPlan:choice.plan,finalAmountMinor:providerDue,checkoutExpiresAt:intent.expires_at});await intents.attachProviderCheckout(intent.id,checkout.id);return checkout}if(provider==='plisio'){const checkout=await plisio.createCheckout({intentId:intent.id,resolvedPlan:choice.plan,finalAmountMinor:providerDue,callbackUrl:await operations.absoluteUrl(req,'/webhooks/plisio'),successUrl:await stateUrl(req,'/account/plisio/return',intent),cancelUrl:await stateUrl(req,'/account/checkout/cancel',intent)});await intents.attachProviderCheckout(intent.id,checkout.id);return checkout}const checkout=await paypal.createCheckout({customerId:req.session.customerId,planCode:choice.planCode,checkoutMode:choice.mode,discountCode:req.body.discountCode||null,returnUrl:await stateUrl(req,'/account/paypal/return',intent),cancelUrl:await stateUrl(req,'/account/checkout/cancel',intent),idempotencyKey:intent.id,checkoutIntentId:intent.id,commercialSnapshot:intent.commercial_snapshot,resolvedPlan:choice.plan,finalAmountMinor:providerDue});await intents.attachProviderCheckout(intent.id,checkout.id);return checkout}catch(error){await intents.consume({intentId:intent.id,nonce:intent.nonce,state:'failed',scope:'customer',provider,ownerId:req.session.customerId}).catch(()=>{});throw error}}
async function finish(req,res,provider){const checkout=await begin(req,res,provider);if(!checkout||res.headersSent)return;if(checkout.internal)return res.redirect('/account?message='+encodeURIComponent(checkout.message));return res.redirect(303,checkout.url)}
function createFlexibleCheckoutRouter(){const router=express.Router();router.use(async(_req,_res,next)=>{try{await providerSettings.ensureLoaded();return next()}catch(error){return next(error)}});router.get('/account/checkout/readiness',requireCustomer,async(_req,res)=>{try{return res.json(await checkoutReadiness())}catch(_){return res.status(503).json({stripe:false,paypal:false,plisio:false})}});router.post('/account/checkout/stripe',requireCustomer,checkoutStartLimit,async(req,res)=>{try{return await finish(req,res,'stripe')}catch(error){return checkoutErrorRedirect(res,error,'Stripe checkout start failed','Stripe checkout could not be started.')}});router.post('/account/checkout/paypal',requireCustomer,checkoutStartLimit,async(req,res)=>{try{return await finish(req,res,'paypal')}catch(error){return checkoutErrorRedirect(res,error,'PayPal checkout start failed','PayPal checkout could not be started.')}});router.post('/account/checkout/plisio',requireCustomer,checkoutStartLimit,async(req,res)=>{try{return await finish(req,res,'plisio')}catch(error){return checkoutErrorRedirect(res,error,'Plisio checkout start failed','Plisio checkout could not be started.')}});router.get('/account/checkout/cancel',requireCustomer,async(req,res)=>{try{await intents.consume({intentId:String(req.query.checkout_intent||''),nonce:String(req.query.checkout_state||''),state:'cancelled',scope:'customer',ownerId:req.session.customerId});return res.redirect('/account?error='+encodeURIComponent('Checkout cancelled.'))}catch(error){return checkoutErrorRedirect(res,error,'Checkout cancel failed','Checkout could not be cancelled.')}});router.post('/account/checkout/cancel-open',requireCustomer,async(req,res)=>{if(!csrf.verify(req))return res.redirect('/account?error='+encodeURIComponent('Invalid or expired security token'));try{const cancelled=await intents.cancelForOwner('customer',req.session.customerId);return res.redirect('/account?message='+encodeURIComponent(cancelled?'Your in-progress checkout was cancelled. You can start again below.':'No in-progress checkout was found.'))}catch(error){return checkoutErrorRedirect(res,error,'Checkout cancel-open failed','Your in-progress checkout could not be cancelled.')}});return router}
module.exports={createFlexibleCheckoutRouter,providerLabel,priceLabel,requireCustomer,stateUrl,stripeSuccessUrl,commercialSnapshot,requestedCurrency,requestedAccessQuantity,choiceQuantity,checkoutReadiness,assertProviderCheckoutReady,livePrimaryRows,overlappingRecurring,CHECKOUT_SAFE};
