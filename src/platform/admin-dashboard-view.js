'use strict';
const {esc}=require('./admin-html');
function when(v){if(!v)return'Never';const d=new Date(v);return Number.isNaN(d.getTime())?'Never':d.toLocaleString()}
function badge(t,k=''){return`<span class="pill ${k}">${esc(t)}</span>`}
function stats(s){return `<div class="metrics"><div class="metric"><div class="metricLabel">Customers</div><div class="metricValue">${s.customers}</div></div><div class="metric"><div class="metricLabel">Active subscriptions</div><div class="metricValue">${s.activeSubscriptions}</div></div><div class="metric"><div class="metricLabel">Resellers</div><div class="metricValue">${s.resellers}</div></div><div class="metric"><div class="metricLabel">Active streams</div><div class="metricValue">${s.activeStreams}</div></div><div class="metric"><div class="metricLabel">Healthy servers</div><div class="metricValue">${s.healthyServers}/${s.servers}</div></div><div class="metric"><div class="metricLabel">Pending requests</div><div class="metricValue">${s.pendingRequests}</div></div></div>`}
function customers(s){const rows=s.recentCustomers.length?s.recentCustomers.map(c=>`<tr><td><a class="mediaTitle" href="/admin/users/${esc(c.id)}">${esc(c.name)}</a><div class="subText">${esc(c.email||'')}</div></td><td>${esc(c.plan_name||'No plan')}</td><td>${badge(c.status||'none',c.status==='active'||c.status==='trialing'?'good':'warn')}</td><td>${esc(c.jellyfin_username||'Not provisioned')}<div class="subText">${esc(c.server_name||'')}</div></td><td>${esc(when(c.current_period_end))}</td></tr>`).join(''):'<tr><td colspan="5" class="empty">No customers yet.</td></tr>';return `<section class="card"><div class="card-header"><h2 class="card-title">Customers</h2><div class="buttonRow"><a class="button" href="/admin/users/new">+ Add customer</a><a class="button secondary" href="/admin/users">View all</a></div></div><div class="tableWrap"><table class="dataTable"><thead><tr><th>Customer</th><th>Plan</th><th>Status</th><th>Jellyfin</th><th>Expiry</th></tr></thead><tbody>${rows}</tbody></table></div></section>`}
function resellers(s){const rows=s.resellerRows.length?s.resellerRows.map(r=>`<tr><td><strong>${esc(r.username)}</strong></td><td>${esc(r.credits)} <span class="subText">+ ${esc(r.trial_credits)} trial</span></td><td>${esc(r.customers)}</td><td>${badge(r.active?'Active':'Disabled',r.active?'good':'bad')}</td></tr>`).join(''):'<tr><td colspan="4" class="empty">No resellers yet.</td></tr>';return `<section class="card"><div class="card-header"><h2 class="card-title">Resellers</h2><a class="button secondary" href="/admin/resellers">Manage</a></div><div class="tableWrap"><table class="dataTable"><thead><tr><th>Reseller</th><th>Credits</th><th>Customers</th><th>Status</th></tr></thead><tbody>${rows}</tbody></table></div></section>`}
function expiring(s){const days=s.options?.expiringWindowDays||3;const rows=s.expiringRows.length?s.expiringRows.map(x=>`<div class="compact-item"><div><a class="compact-title" href="/admin/users/${esc(x.id)}">${esc(x.name)}</a><div class="compact-meta">${esc(x.plan_name||'Plan')}</div></div><div class="right"><span class="pill warn">${esc(when(x.current_period_end))}</span></div></div>`).join(''):'<div class="empty">No customers expiring soon.</div>';return `<section class="card"><div class="card-header"><h2 class="card-title">Expiring within ${esc(days)} day${Number(days)===1?'':'s'}</h2></div><div class="card-body">${rows}</div></section>`}
function requests(s){const rows=s.requestRows.length?s.requestRows.map(x=>`<div class="compact-item"><div><div class="compact-title">${esc(x.title)}</div><div class="compact-meta">${esc(x.media_type||'other')} · ${esc(x.requester)} · ${esc(when(x.created_at))}</div></div></div>`).join(''):'<div class="empty">No pending requests.</div>';return `<section class="card"><div class="card-header"><h2 class="card-title">Content requests</h2><a class="button secondary" href="/admin/requests">Review</a></div><div class="card-body">${rows}</div></section>`}
function serverHealth(s){const rows=s.serverRows.length?s.serverRows.map(x=>`<div class="compact-item"><div><div class="compact-title">${esc(x.name)}</div><div class="compact-meta">${esc(x.server_class)} · ${esc(x.assigned_users)} users · ${esc(x.active_streams)} streams</div></div><div class="right">${badge(x.health_status,x.health_status==='healthy'?'good':x.health_status==='offline'?'bad':'warn')}<div class="compact-meta">${esc(when(x.last_health_check))}</div></div></div>`).join(''):'<div class="empty">No servers configured.</div>';return `<section class="card"><div class="card-header"><h2 class="card-title">Server health</h2><a class="button secondary" href="/admin/servers">Manage</a></div><div class="card-body">${rows}</div></section>`}
function quickActions(){return `<section class="card"><div class="card-header"><h2 class="card-title">Quick actions</h2></div><div class="card-body"><div class="quick-actions"><a class="quick-action" href="/admin/users/new"><strong>+ Customer</strong><span>Create subscription + Jellyfin access</span></a><a class="quick-action" href="/admin/resellers"><strong>+ Reseller</strong><span>Credits and reseller access</span></a><a class="quick-action" href="/admin/plans"><strong>Plans</strong><span>Pricing and permissions</span></a><a class="quick-action" href="/admin/servers/new"><strong>+ Server</strong><span>Add Jellyfin capacity</span></a><a class="quick-action" href="/admin/settings"><strong>Settings</strong><span>Store and service configuration</span></a><a class="quick-action" href="/admin/notifications"><strong>Notifications</strong><span>Alerts and preferences</span></a></div></div></section>`}
function operational(s){return `<section class="card"><div class="card-header"><h2 class="card-title">Operational status</h2><a class="button secondary" href="/admin/activity">Open activity</a></div><div class="card-body"><div class="settings-grid"><div><div class="stat-label">Transcodes now</div><div class="metricMini">${s.transcodes}</div></div><div><div class="stat-label">Policy observations · 24h</div><div class="metricMini">${s.wouldStop24h}</div></div><div><div class="stat-label">Payment errors · 24h</div><div class="metricMini">${s.paymentFailures24h}</div></div><div><div class="stat-label">Safety skips · 24h</div><div class="metricMini">${s.safetySkips24h}</div></div></div></div></section>`}
function renderDashboard(s){return `${stats(s)}<div class="content-grid"><div class="stack">${customers(s)}${resellers(s)}${operational(s)}</div><aside class="stack">${quickActions()}${expiring(s)}${requests(s)}${serverHealth(s)}<section class="card"><div class="card-header"><h2 class="card-title">Reseller credits</h2></div><div class="card-body"><div class="metrics"><div class="metric"><div class="metricLabel">Regular</div><div class="metricValue small">${s.credits}</div></div><div class="metric"><div class="metricLabel">Trial</div><div class="metricValue small">${s.trialCredits}</div></div></div></div></section></aside></div>`}
module.exports={renderDashboard};