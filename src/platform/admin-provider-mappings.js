'use strict';
const express=require('express');
const {query}=require('../db');
const runtimeSettings=require('./runtime-settings');
const {layout,esc}=require('./admin-html');
function gate(req,res,next){return req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId?next():res.redirect('/login?session=expired');}
function noStore(_q,res,next){res.setHeader('Cache-Control','no-store, private, max-age=0');res.setHeader('Pragma','no-cache');next();}
function statePill(x){return `<span class="pill ${x.validation_state==='verified'?'good':x.validation_state==='failed'?'bad':'warn'}">${esc(x.validation_state||'unverified')}</span>${x.active?'':'<div class="subText">inactive until verified</div>'}`;}
function money(x){try{return new Intl.NumberFormat('en-GB',{style:'currency',currency:x.currency,currencyDisplay:'narrowSymbol'}).format(Number(x.price_minor||0)/100)}catch(_){return `${x.currency} ${(Number(x.price_minor||0)/100).toFixed(2)}`}}
async function page(req){await runtimeSettings.ensureLoaded();const direct=await query(`SELECT pp.*,p.name object_name,p.code object_code,pr.price_minor,pr.currency,pr.is_default FROM plan_provider_prices pp JOIN plan_prices pr ON pr.id=pp.plan_price_id AND pr.plan_id=pp.plan_id JOIN plans p ON p.id=pp.plan_id ORDER BY p.name,pr.currency,pp.provider,pp.checkout_mode`),row=(x,scope)=>`<tr><td>${esc(scope)}</td><td><strong>${esc(x.object_name)}</strong><div class="subText">${esc(x.object_code||'')} · ${esc(money(x))}${x.is_default?' · default':''}</div></td><td>${esc(x.provider)}</td><td><code>${esc(x.external_id||'provider amount')}</code><div class="subText">${esc(x.checkout_mode||'monthly subscription')}</div></td><td>${statePill(x)}${x.validation_error?`<div class="subText">${esc(x.validation_error)}</div>`:''}</td><td>${x.validated_at?esc(new Date(x.validated_at).toLocaleString('en-GB')):'—'}</td></tr>`,body=`${req.query.message?`<div class="notice success">${esc(req.query.message)}</div>`:''}${req.query.error?`<div class="notice error">${esc(req.query.error)}</div>`:''}<div class="operatorCallout"><strong>Diagnostic inventory only.</strong> Configure and verify Stripe/PayPal mappings from the owning plan: <a href="/admin/plans"><strong>Plans & Storefront → Plan → Commerce</strong></a>. This page is retained for cross-plan troubleshooting and migration diagnostics.</div><section class="section"><div class="sectionHead"><div><h2>Provider mapping inventory</h2><div class="muted">Read-only view of the provider IDs currently attached to plan price variants.</div></div><a class="button secondary btn-sm" href="/admin/plans">Open plans</a></div>${direct.rowCount?`<div class="tableWrap"><table class="dataTable responsiveTable"><thead><tr><th>Scope</th><th>Product / currency</th><th>Provider</th><th>Remote mapping</th><th>Validation</th><th>Last checked</th></tr></thead><tbody>${direct.rows.map(x=>row(x,'Direct plan')).join('')}</tbody></table></div>`:'<div class="empty">No provider mappings are configured.</div>'}</section>`;return layout({siteName:runtimeSettings.siteName(),active:'provider-mappings',title:'Provider mapping diagnostics',subtitle:'Read-only cross-plan provider mapping inventory',body});}
function createAdminProviderMappingsRouter(){const r=express.Router();r.use('/admin/provider-mappings',gate,noStore);r.get('/admin/provider-mappings',async(req,res,next)=>{try{return res.send(await page(req));}catch(e){next(e);}});r.post('/admin/provider-mappings/direct/:id/validate',(_req,res)=>res.redirect('/admin/provider-mappings?error='+encodeURIComponent('Provider mappings are managed from the owning plan. Open Plans & Storefront, choose the plan, then use its Commerce payment mappings.')));return r;}
module.exports={createAdminProviderMappingsRouter,page};
