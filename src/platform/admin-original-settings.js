'use strict';
const express=require('express');
const {query}=require('../db');
const csrf=require('../auth/csrf');
const {layout,esc}=require('./admin-html');
function gate(req,res,next){if(req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId)return next();return res.redirect('/login?session=expired')}
function notice(v,k=''){return v?`<div class="notice ${k}">${esc(v)}</div>`:''}
function csrfInput(req){return `<input type="hidden" name="_csrf" value="${esc(csrf.token(req))}">`}
async function load(){const [r,p]=await Promise.all([query("SELECT setting_key,setting_value FROM platform_settings WHERE setting_key IN ('storefront','storefront_features','reseller_defaults','admin_defaults')"),query("SELECT code,name FROM plans WHERE active=TRUE AND audience IN ('direct','both') ORDER BY sort_order,price_minor,name")]);const m=Object.fromEntries(r.rows.map(x=>[x.setting_key,x.setting_value]));const fallback=p.rows[0]?.code||'monthly';return{store:m.storefront||{},features:Array.isArray(m.storefront_features)?m.storefront_features:[],reseller:{credits:0,trialCredits:0,...(m.reseller_defaults||{})},admin:{defaultPlanCode:fallback,defaultServerClass:'premium',defaultServerPriority:100,defaultServerMaxUsers:0,expiringWindowDays:3,recentCustomerLimit:12,...(m.admin_defaults||{})},plans:p.rows}}
function status(label,value,kind='accent',hint=''){return `<div class="compact-item"><div><div class="compact-title">${esc(label)}</div><div class="compact-meta">${esc(hint)}</div></div><span class="pill ${kind}">${esc(value)}</span></div>`}
function page(req,d){const s=d.store,f=d.features,r=d.reseller,a=d.admin;const planOptions=d.plans.map(p=>`<option value="${esc(p.code)}" ${a.defaultPlanCode===p.code?'selected':''}>${esc(p.name)} · ${esc(p.code)}</option>`).join('');const body=`${notice(req.query.message,'success')}${notice(req.query.error,'error')}<div class="settings-grid"><section class="settings-card"><div class="card-header"><div><h3>Storefront</h3><div class="settings-hint">Public homepage copy and support details</div></div><a class="button secondary" href="/" target="_blank" rel="noopener">Preview</a></div><div class="card-body"><form method="post" action="/admin/settings/storefront">${csrfInput(req)}<div class="formGroup"><label>Hero title</label><input class="input" name="heroTitle" maxlength="140" value="${esc(s.heroTitle||'')}"></div><div class="formGroup"><label>Hero subtitle</label><textarea class="input" name="heroSubtitle" maxlength="500">${esc(s.heroSubtitle||'')}</textarea></div><div class="formGrid"><div class="formGroup"><label>Features heading</label><input class="input" name="featureTitle" value="${esc(s.featureTitle||'')}"></div><div class="formGroup"><label>Support email</label><input class="input" type="email" name="supportEmail" value="${esc(s.supportEmail||'')}"></div></div><div class="formGroup"><label>Announcement</label><input class="input" name="announcement" maxlength="200" value="${esc(s.announcement||'')}"></div><div class="formGroup"><label>Features · one per line</label><textarea class="input" name="features" rows="7">${esc(f.join('\n'))}</textarea></div><div class="formGroup"><label>Verification code <span class="muted">(only when enabled)</span></label><input class="input" name="code" autocomplete="one-time-code"></div><button class="button">Save storefront</button></form></div></section><section class="settings-card"><div class="card-header"><div><h3>Admin defaults</h3><div class="settings-hint">Defaults for manual customer and server workflows</div></div></div><div class="card-body"><form method="post" action="/admin/settings/admin-defaults">${csrfInput(req)}<div class="formGrid"><div class="formGroup"><label>Default customer plan</label><select class="input" name="defaultPlanCode">${planOptions||'<option value="">No active direct plans</option>'}</select></div><div class="formGroup"><label>Default server class</label><select class="input" name="defaultServerClass">${['premium','free','custom'].map(x=>`<option ${a.defaultServerClass===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="formGroup"><label>Default server priority</label><input class="input" type="number" min="0" max="10000" name="defaultServerPriority" value="${esc(a.defaultServerPriority)}"></div><div class="formGroup"><label>Default max users · 0 = unlimited</label><input class="input" type="number" min="0" max="100000" name="defaultServerMaxUsers" value="${esc(a.defaultServerMaxUsers)}"></div><div class="formGroup"><label>Expiring-soon window · days</label><input class="input" type="number" min="1" max="30" name="expiringWindowDays" value="${esc(a.expiringWindowDays)}"></div><div class="formGroup"><label>Recent customers on dashboard</label><input class="input" type="number" min="5" max="50" name="recentCustomerLimit" value="${esc(a.recentCustomerLimit)}"></div></div><button class="button">Save admin defaults</button></form></div></section><section class="settings-card"><div class="card-header"><div><h3>Reseller defaults</h3><div class="settings-hint">Starting balances for newly created resellers</div></div><a class="button secondary" href="/admin/reseller-management">Manage resellers</a></div><div class="card-body"><form method="post" action="/admin/settings/reseller-defaults">${csrfInput(req)}<div class="formGrid"><div class="formGroup"><label>Regular credits</label><input class="input" type="number" min="0" max="100000" name="credits" value="${esc(r.credits)}"></div><div class="formGroup"><label>Trial credits</label><input class="input" type="number" min="0" max="20" name="trialCredits" value="${esc(r.trialCredits)}"></div></div><button class="button">Save reseller defaults</button></form></div></section><section class="settings-card"><div class="card-header"><div><h3>Deployment & integrations</h3><div class="settings-hint">Sensitive values remain server-side</div></div></div><div class="card-body">${status('Public registration',process.env.PUBLIC_REGISTRATION!=='false'?'Enabled':'Disabled',process.env.PUBLIC_REGISTRATION!=='false'?'good':'warn','Deployment-level account creation gate')}${status('Email verification',process.env.REQUIRE_EMAIL_VERIFICATION==='true'?'Required':'Optional','accent','Deployment-level authentication policy')}${status('Stripe',process.env.STRIPE_API_KEY||process.env.STRIPE_RESTRICTED_KEY?'Configured':'Not configured',process.env.STRIPE_API_KEY||process.env.STRIPE_RESTRICTED_KEY?'good':'warn','Secrets are never rendered here')}${status('PayPal',process.env.PAYPAL_CLIENT_ID&&process.env.PAYPAL_CLIENT_SECRET?'Configured':'Not configured',process.env.PAYPAL_CLIENT_ID&&process.env.PAYPAL_CLIENT_SECRET?'good':'warn','Secrets are never rendered here')}${status('Telegram',process.env.TELEGRAM_BOT_TOKEN&&process.env.TELEGRAM_CHAT_ID?'Configured':'Not configured',process.env.TELEGRAM_BOT_TOKEN&&process.env.TELEGRAM_CHAT_ID?'good':'warn','Configure credentials on the server')}${status('Activity mode',String(process.env.STREAM_POLICY_MODE||'observe').toUpperCase(),'accent','Playback-policy worker mode')}<div class="quick-actions" style="margin-top:12px"><a class="quick-action" href="/admin/plans"><strong>Plans</strong><span>Prices, streams and reseller credits</span></a><a class="quick-action" href="/admin/payments"><strong>Payments</strong><span>Provider status and events</span></a><a class="quick-action" href="/admin/notifications"><strong>Notifications</strong><span>Event delivery preferences</span></a><a class="quick-action" href="/admin/security"><strong>Security</strong><span>Sessions and account controls</span></a></div></div></section></div>`;return layout({siteName:process.env.SITE_NAME||'CAPTaINFiN',active:'settings',title:'Settings',subtitle:'Business defaults, storefront and integration state',body})}
function int(v,min,max,f){const n=parseInt(v,10);return Number.isFinite(n)&&n>=min&&n<=max?n:f}
function createAdminOriginalSettingsRouter(){const r=express.Router();r.use('/admin/settings',gate);r.get('/admin/settings',async(req,res,next)=>{try{res.setHeader('Cache-Control','no-store, private, max-age=0');return res.send(page(req,await load()))}catch(e){next(e)}});r.post('/admin/settings/admin-defaults',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');const requested=String(req.body.defaultPlanCode||'');const available=await query("SELECT code FROM plans WHERE active=TRUE AND audience IN ('direct','both') ORDER BY CASE WHEN code=$1 THEN 0 ELSE 1 END,sort_order,price_minor,name LIMIT 1",[requested]);const allowedPlan=available.rows[0]?.code||'monthly';const serverClass=['premium','free','custom'].includes(req.body.defaultServerClass)?req.body.defaultServerClass:'premium';const value={defaultPlanCode:allowedPlan,defaultServerClass:serverClass,defaultServerPriority:int(req.body.defaultServerPriority,0,10000,100),defaultServerMaxUsers:int(req.body.defaultServerMaxUsers,0,100000,0),expiringWindowDays:int(req.body.expiringWindowDays,1,30,3),recentCustomerLimit:int(req.body.recentCustomerLimit,5,50,12)};await query(`INSERT INTO platform_settings(setting_key,setting_value,updated_by,updated_at) VALUES('admin_defaults',$1::jsonb,$2,NOW()) ON CONFLICT(setting_key) DO UPDATE SET setting_value=EXCLUDED.setting_value,updated_by=EXCLUDED.updated_by,updated_at=NOW()`,[JSON.stringify(value),req.session.authUserId]);return res.redirect('/admin/settings?message='+encodeURIComponent('Admin defaults saved.'))});r.post('/admin/settings/reseller-defaults',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');const value={credits:int(req.body.credits,0,100000,0),trialCredits:int(req.body.trialCredits,0,20,0)};await query(`INSERT INTO platform_settings(setting_key,setting_value,updated_by,updated_at) VALUES('reseller_defaults',$1::jsonb,$2,NOW()) ON CONFLICT(setting_key) DO UPDATE SET setting_value=EXCLUDED.setting_value,updated_by=EXCLUDED.updated_by,updated_at=NOW()`,[JSON.stringify(value),req.session.authUserId]);return res.redirect('/admin/settings?message='+encodeURIComponent('Reseller defaults saved.'))});return r}
module.exports={createAdminOriginalSettingsRouter};