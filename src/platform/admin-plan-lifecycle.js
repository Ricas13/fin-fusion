'use strict';
const express=require('express');
const {query}=require('../db');
const csrf=require('../auth/csrf');
const runtimeSettings=require('./runtime-settings');
const planPolicy=require('../entitlements/plan-lifecycle-policy');
const lifecyclePolicy=require('../entitlements/jellyfin-lifecycle-policy');
const checkboxForm=require('./admin-checkbox-form');
const {esc,layout}=require('./admin-html');
function gate(req,res,next){return req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId?next():res.redirect('/login?session=expired');}
function noStore(_req,res,next){res.setHeader('Cache-Control','no-store, private, max-age=0');res.setHeader('Pragma','no-cache');next();}
function token(req){return `<input type="hidden" name="_csrf" value="${esc(csrf.token(req))}">`;}
function notice(req){return `${req.query.message?`<div class="notice success">${esc(req.query.message)}</div>`:''}${req.query.error?`<div class="notice error">${esc(req.query.error)}</div>`:''}`;}
function checked(v){return v?'checked':'';}
function toggle(name,label,value,help=''){return `<label class="toggleRow"><input type="checkbox" name="${esc(name)}" ${checked(value)}><span><strong>${esc(label)}</strong>${help?`<small class="muted">${esc(help)}</small>`:''}</span></label>`;}
async function load(planId){const r=await query(`SELECT id,code,name,price_minor,currency,billing_interval,service_type,inactivity_policy FROM plans WHERE id=$1 AND archived_at IS NULL`,[planId]);return r.rows[0]||null;}
async function page(req){await runtimeSettings.ensureLoaded();const [plan,global]=await Promise.all([load(req.params.id),lifecyclePolicy.get()]);if(!plan)return null;const raw=plan.inactivity_policy||{},p=planPolicy.effectiveForFreePlan(raw,global),jellyfin=['jellyfin','bundle'].includes(String(plan.service_type||'jellyfin')),free=Number(plan.price_minor||0)===0&&String(plan.billing_interval||'')!=='trial',freeJellyfin=free&&jellyfin,category=lifecyclePolicy.categoryFor({billingInterval:plan.billing_interval,priceMinor:plan.price_minor}),inheritedDelete=lifecyclePolicy.deleteDays(global,category,{inactivity_policy:{}}).days,triggers=[];if(p.noPlaybackDays!=null)triggers.push(`No playback for ${p.noPlaybackDays} day${p.noPlaybackDays===1?'':'s'}`);if(p.minimumPlaybackMinutes!=null)triggers.push(`Under ${p.minimumPlaybackMinutes} minute${p.minimumPlaybackMinutes===1?'':'s'} in ${p.playbackWindowDays} day${p.playbackWindowDays===1?'':'s'}`);const inheritsAll=p.inherited&&p.inherited.enabled&&p.inherited.dryRun&&p.inherited.noPlaybackDays,triggerText=freeJellyfin?(p.enabled?(triggers.length?triggers.join(' · '):'Enabled, but no usage trigger is configured yet'):'Usage rules disabled'):(category==='trial'?'Trial entitlement expires':'Paid entitlement is no longer valid'),sourceText=inheritsAll?'Using global Free Server defaults':'Plan-specific lifecycle settings';const body=`${notice(req)}
<div class="operatorCallout statusInfo"><strong>Portal identity is never automated away.</strong> These settings affect the Jellyfin user only. CAPTAiNFiN portal access remains until an administrator bans or deletes the portal account.</div>
<section class="section"><div class="sectionHead"><div><h2>Jellyfin lifecycle override</h2><div class="muted">${free?'Free plans inherit the global inactivity rule until you save a plan-specific override.':'This plan is disabled when its entitlement lapses; you can override the post-disable deletion grace.'}</div></div></div>
<form class="formPanel" method="post" action="/admin/plans/${esc(plan.id)}/lifecycle">${token(req)}
${freeJellyfin?`<input type="hidden" name="_lifecycleCheckboxes" value="1"><div class="operatorCallout"><strong>Free Server Plan rules:</strong> current effective source: ${esc(sourceText)}. Saving this form creates a plan-specific override.</div><div class="toggleGrid">${toggle('enabled','Enable usage rules',p.enabled,'Uncheck to explicitly exempt this plan from the global Free Server inactivity rule.')}${toggle('dryRun','Dry run only',p.dryRun,'Global dry-run always wins; a plan cannot force enforcement while the global policy is dry-run.')}</div>`:''}
<div class="formGrid">
${freeJellyfin?`<div class="formGroup"><label for="lifecycle-no-playback-days">No playback for</label><div class="inputUnit"><input id="lifecycle-no-playback-days" class="input" type="number" name="noPlaybackDays" min="1" max="3650" value="${esc(p.noPlaybackDays??'')}"><span>days</span></div><div class="inlineHelp">${p.inherited?.noPlaybackDays?`Currently inherited from the global Free Server default (${esc(global.freeNoPlaybackDays)} days).`:'Blank = disable the no-playback trigger for this plan.'}</div></div><div class="formGroup"><label for="lifecycle-minimum-playback-minutes">Minimum playback</label><div class="inputUnit"><input id="lifecycle-minimum-playback-minutes" class="input" type="number" name="minimumPlaybackMinutes" min="1" max="1000000" value="${esc(p.minimumPlaybackMinutes??'')}" placeholder="30"><span>minutes</span></div><label for="lifecycle-playback-window-days">Within</label><div class="inputUnit"><input id="lifecycle-playback-window-days" class="input" type="number" name="playbackWindowDays" min="1" max="365" value="${esc(p.playbackWindowDays)}"><span>days</span></div><div class="inlineHelp">Blank minimum = do not use the minimum-playback trigger.</div></div><div class="formGroup"><label for="lifecycle-minimum-observation-hours">Minimum observation</label><div class="inputUnit"><input id="lifecycle-minimum-observation-hours" class="input" type="number" name="minimumObservationHours" min="1" max="2160" value="${esc(p.minimumObservationHours)}"><span>hours</span></div><div class="inlineHelp">Prevents brand-new free users from being disabled before enough evidence exists.</div></div>`:''}
<div class="formGroup"><label for="lifecycle-delete-after-disable-days">Delete Jellyfin user after being disabled</label><div class="inputUnit"><input id="lifecycle-delete-after-disable-days" class="input" type="number" name="deleteAfterDisableDays" min="1" max="3650" value="${esc(p.deleteAfterDisableDays??'')}" placeholder="${esc(inheritedDelete)}"><span>days</span></div><div class="inlineHelp">Blank = inherit global ${esc(inheritedDelete)} days for ${esc(category)} access.</div></div>
</div>
<button class="button" ${jellyfin?'':'disabled'}>Save lifecycle override</button> <a class="button secondary" href="/admin/settings/jellyfin-lifecycle">Global defaults</a></form></section>
<section class="section"><div class="sectionHead"><div><h2>Effective behavior</h2></div></div><div class="card-body">${freeJellyfin?`<div class="compact-item"><div><div class="compact-title">Policy source</div><div class="compact-meta">${esc(sourceText)}</div></div><span class="pill ${inheritsAll?'good':'accent'}">${inheritsAll?'Inherited':'Override'}</span></div>`:''}<div class="compact-item"><div><div class="compact-title">Disable trigger</div><div class="compact-meta">${esc(triggerText)}</div></div><span class="pill good">Jellyfin only</span></div>${freeJellyfin?`<div class="compact-item"><div><div class="compact-title">Enforcement mode</div><div class="compact-meta">${p.dryRun?'Dry run: evidence is recorded without disabling access':'Enforce: matching users are disabled after telemetry checks'}</div></div><span class="pill ${p.dryRun?'warn':'good'}">${p.dryRun?'Dry run':'Enforce'}</span></div>`:''}<div class="compact-item"><div><div class="compact-title">Delete trigger</div><div class="compact-meta">${esc(p.deleteAfterDisableDays??inheritedDelete)} days after Jellyfin disable</div></div><span class="pill warn">Portal preserved</span></div></div></section>`;return layout({siteName:runtimeSettings.siteName(),active:'plans',title:`${plan.name} · Lifecycle`,subtitle:'Plan-specific Jellyfin lifecycle override',body,action:`<a class="button secondary" href="/admin/plans/${esc(plan.id)}/edit">Plan settings</a> <a class="button secondary" href="/admin/plans">Back to Plans</a>`});}
function lifecycleFormInput(body={}){return checkboxForm.explicitCheckboxes(body,'_lifecycleCheckboxes',['enabled','dryRun']);}
function createAdminPlanLifecycleRouter(){const r=express.Router();r.get('/admin/plans/:id/stremio',gate,noStore,async(req,res)=>{try{const plan=await load(req.params.id);if(!plan)return res.status(404).send('Plan not found');if(!['stremio','bundle'].includes(String(plan.service_type||'')))return res.redirect(`/admin/plans/${encodeURIComponent(plan.id)}/edit?error=${encodeURIComponent('This plan does not include Stremio.')}`);return res.redirect(`/admin/servers/stremio?plan=${encodeURIComponent(plan.id)}&message=${encodeURIComponent(`${plan.name}: Stremio sources, libraries and runtime are managed in the Stremio control centre.`)}`);}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/edit?error=${encodeURIComponent(error.message||'Stremio settings could not be opened.')}`);}});r.use('/admin/plans/:id/lifecycle',gate,noStore);r.get('/admin/plans/:id/lifecycle',async(req,res,next)=>{try{const html=await page(req);return html?res.send(html):res.status(404).send('Plan not found');}catch(error){next(error)}});r.post('/admin/plans/:id/lifecycle',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{await planPolicy.save(req.params.id,lifecycleFormInput(req.body),req.session.authUserId);return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/lifecycle?message=${encodeURIComponent('Jellyfin lifecycle override saved.')}`);}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/lifecycle?error=${encodeURIComponent(error.message)}`)}});return r;}
module.exports={createAdminPlanLifecycleRouter,page,lifecycleFormInput};
