'use strict';
const express=require('express');
const intents=require('../payments/checkout-intents');
const paypal=require('../payments/paypal');
const stripe=require('../payments/stripe');
const csrf=require('../auth/csrf');
function requireCustomer(req,res,next){return req.session?.customerId&&req.session?.customerUserId?next():res.redirect('/account/login?next='+encodeURIComponent(req.originalUrl||'/account'))}
function absoluteUrl(req,path){const proto=req.get('x-forwarded-proto')?.split(',')[0]?.trim()||req.protocol;const host=req.get('x-forwarded-host')||req.get('host');return `${proto}://${host}${path}`}
function sameOrigin(req){const fetchSite=String(req.get('sec-fetch-site')||'');if(fetchSite==='cross-site')return false;const origin=req.get('origin');if(!origin)return fetchSite!=='cross-site';try{return new URL(origin).host===(req.get('x-forwarded-host')||req.get('host'))}catch{return false}}
function mutationGuard(req,res,next){if(csrf.verify(req)||sameOrigin(req))return next();return res.status(403).send('Invalid or cross-site request')}
function createCustomerPaymentReturnRouter(){const r=express.Router();r.get('/account/paypal/return',requireCustomer,async(req,res)=>{try{const intentId=String(req.query.checkout_intent||''),state=String(req.query.checkout_state||'');const row=await intents.verify({intentId,nonce:state,scope:'customer',provider:'paypal',ownerId:req.session.customerId});const providerId=String(row.checkout_mode==='payment'?(req.query.token||row.provider_checkout_id):(req.query.subscription_id||row.provider_checkout_id)||'');if(!providerId||String(row.provider_checkout_id)!==providerId)throw new Error('PayPal return does not match the checkout that was started.');if(row.checkout_mode==='payment')await paypal.captureOrder(providerId);else await paypal.activateSubscription(providerId);await intents.consume({intentId,nonce:state,providerCheckoutId:providerId,state:'completed',scope:'customer',provider:'paypal',ownerId:req.session.customerId});return res.redirect('/account?message='+encodeURIComponent('PayPal payment completed.'))}catch(error){return res.redirect('/account?error='+encodeURIComponent(error.message))}});r.post('/account/stripe/portal',requireCustomer,mutationGuard,async(req,res)=>{try{const portal=await stripe.createCustomerPortal({customerId:req.session.customerId,returnUrl:absoluteUrl(req,'/account')});return res.redirect(303,portal.url)}catch(error){return res.redirect('/account?error='+encodeURIComponent(error.message))}});return r}
module.exports={createCustomerPaymentReturnRouter,mutationGuard,sameOrigin};
