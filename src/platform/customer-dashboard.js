'use strict';
const express=require('express');
const {query}=require('../db');
const customers=require('../customers');
const stripe=require('../payments/stripe');
const paypal=require('../payments/paypal');
const plisio=require('../payments/plisio');
const planPricing=require('../payments/plan-pricing');
const accessVariants=require('../payments/stream-variants');
const provisioning=require('../jellyfin/resilient-provisioning');
const subscriptionState=require('../entitlements/subscription-state');
const stremioEntitlements=require('../stremio/entitlements');
const installRecovery=require('../stremio/install-credential-recovery');
const permanentAccess=require('../entitlements/permanent-access');
const cleanupReturn=require('../entitlements/jellyfin-cleanup-return');
const requestUserSync=require('../integrations/request-user-sync');
const runtimeSettings=require('./runtime-settings');
const operations=require('./operations-settings');
const customerNav=require('./customer-nav-html');
const productReadiness=require('./product-readiness');
const planCapacity=require('../entitlements/plan-capacity');
const checkoutIntents=require('../payments/checkout-intents');
const csrf=require('../auth/csrf');
function requireCustomer(req,res,next){return req.session?.customerId&&req.session?.customerUserId?next():res.redirect('/account/login?next='+encodeURIComponent(req.originalUrl||'/account'));}
async function hideInternalAccounts(_customerId,portal){if(!portal||!Array.isArray(portal.accounts))return portal;portal.accounts=portal.accounts.filter(account=>String(account.account_purpose||'jellyfin')!=='stremio_internal');return portal;}
function deliveryType(entitlement){return productReadiness.serviceType({service_type:entitlement?.service_type_snapshot||entitlement?.service_type||'jellyfin'});}
function fallbackCapacity(plan){return{limit:plan.capacity_limit??null,used:0,reserved:0,remaining:plan.capacity_limit??null,soldOut:false,label:'Available',kind:'available'};}
async function sellablePlans(){const currency=await planPricing.platformDefaultCurrency(),logical=await customers.listPublicPlans(),priced=await planPricing.decoratePlans(logical,null),decorated=await accessVariants.decoratePlans(priced,currency),ctx=await productReadiness.context(),ready=decorated.filter(plan=>productReadiness.evaluate(plan,ctx).sellable),enriched=[];for(const plan of ready){const capacity=await planCapacity.usage(plan.id).catch(()=>fallbackCapacity(plan));let variants=Array.isArray(plan.access_variants)?plan.access_variants:[];if(variants.length)variants=await Promise.all(variants.map(async variant=>{const quantity=Number(variant.access_quantity||variant.quantity||1),capacityOptions=variant.variant_kind==='households'?{households:quantity}:{streams:quantity};return{...variant,capacity:await planCapacity.usage(plan.id,undefined,capacityOptions).catch(()=>capacity)};}));enriched.push({...plan,capacity,access_variants:variants});}return enriched.filter(plan=>plan.is_free_tier||!plan.capacity.soldOut);}
function accountForEntitlement(portal,entitlement){if(!Array.isArray(portal?.accounts)||!entitlement)return null;const lane=entitlement.is_free_tier?'free':'primary';return portal.accounts.find(a=>a.access_lane===lane&&!a.disabled)||portal.accounts.find(a=>a.access_lane===lane)||null;}
function onboardingMessage(portal,currentPlan){if(!currentPlan||!['jellyfin','bundle'].includes(deliveryType(currentPlan)))return null;const account=accountForEntitlement(portal,currentPlan);if(!account||account.disabled||account.last_activity_at)return null;const username=account.jellyfin_username||portal.customer?.login_username||'your Jellyfin username';if(account.password_setup_required)return 'Your Jellyfin account is ready. Choose your password below to start watching.';return `Your Jellyfin account is ready. Open Jellyfin and sign in as ${username}.`;}
function customerProvisioningMessage(state){const message=String(state?.last_error||'');if(/no eligible jellyfin server|no jellyfin server|no suitable server/i.test(message))return 'No suitable Jellyfin server is available for this plan right now. We will retry automatically, or you can retry now.';if(/username .* already exists|target_username_exists/i.test(message))return 'That Jellyfin username is already in use on the target server. Please retry; if it continues, contact support.';if(/capacity|max_users|sold out/i.test(message))return 'The eligible Jellyfin server is currently at capacity. We will retry automatically when space is available.';if(state&&['blocked','failed','pending','running'].includes(String(state.status||'')))return 'One of your streaming services is still being prepared. CAPTAiNFiN will keep retrying automatically.';return null;}
function stremioDeepLink(manifestUrl){if(!manifestUrl)return null;const url=new URL(manifestUrl);return `stremio://${url.host}${url.pathname}${url.search}`;}
async function stremioLinks(req,customerId,hasStremio){if(!hasStremio)return{manifestUrl:null,installUrl:null};const recovered=await installRecovery.current(customerId).catch(()=>null);if(!recovered?.credential)return{manifestUrl:null,installUrl:null};const manifestUrl=await operations.absoluteUrl(req,`/stremio/${encodeURIComponent(recovered.credential)}/manifest.json`);return{manifestUrl,installUrl:stremioDeepLink(manifestUrl)};}
function createCustomerDashboardRouter(){const r=express.Router();r.get('/account',requireCustomer,async(req,res,next)=>{try{await runtimeSettings.ensureLoaded();const customerId=req.session.customerId,restored=await cleanupReturn.restoreReturningCustomer(customerId,{reconcile:provisioning.reconcileCustomer}).catch(error=>({restored:false,error:error.message})),currency=await planPricing.platformDefaultCurrency(),[portalRaw,plans,currentPlan,freePlan,stremioPlan,requestAccess,requestConfig,rawProvisioningState,permanentState]=await Promise.all([customers.getCustomerPortal(customerId),sellablePlans(),provisioning.currentEntitlement(customerId),subscriptionState.liveFreeJellyfinSubscription(customerId,{includeBlocked:true}),stremioEntitlements.entitledSubscription(customerId),requestUserSync.requestAccessForCustomer(customerId),requestUserSync.configuration(),provisioning.control.getCustomerState(customerId).catch(()=>null),permanentAccess.status(customerId).catch(()=>null)]),portal=await hideInternalAccounts(customerId,portalRaw),navOptions=customerNav.optionsFromPortal(portal),restoreMessage=restored.restored?'Your previous inactive Jellyfin profile was cleaned up. Because you returned, CAPTAiNFiN has prepared fresh access for you.':null,paymentFlags={stripeEnabled:stripe.enabled(),paypalEnabled:paypal.enabled(),plisioEnabled:plisio.enabled()};if(!currentPlan&&!freePlan&&!stremioPlan){const openCheckout=await checkoutIntents.getOpenForOwner('customer',customerId).catch(()=>null);return res.render('customer/onboarding',{portal,plans,...paymentFlags,currency,openCheckout,navOptions,csrfToken:csrf.token(req),siteName:runtimeSettings.siteName(),message:req.query.message||restoreMessage||null,error:req.query.error||restored.error||null});}const jellyfinPlan=currentPlan||freePlan||null,delivery=deliveryType(jellyfinPlan),hasJellyfin=Boolean(jellyfinPlan&&['jellyfin','bundle'].includes(delivery)),hasStremio=Boolean(stremioPlan),links=await stremioLinks(req,customerId,hasStremio),isPermanent=Boolean(jellyfinPlan?.is_free_tier)||Boolean(permanentState?.active&&String(permanentState.subscription_id)===String(jellyfinPlan?.subscription_id)),provisioningState=rawProvisioningState?{...rawProvisioningState,last_error:customerProvisioningMessage(rawProvisioningState)}:null,effective=jellyfinPlan&&hasJellyfin?await provisioning.effectivePolicyForCustomer(customerId,jellyfinPlan).catch(()=>null):null,libraryEntitlement=effective?effective.entitlementRows.filter(row=>row.effective).map(row=>row.name):[],librarySelection=effective?effective.visibleNames:[],welcome=onboardingMessage(portal,jellyfinPlan),message=req.query.message||restoreMessage||welcome||null;return res.render('customer/dashboard',{portal,plans,currentPlan:jellyfinPlan,freePlan,stremioPlan,...paymentFlags,currency,navOptions,overseerrUrl:runtimeSettings.overseerrUrl(),requestAccess,requestSyncConfigured:requestConfig.configured,libraryEntitlement,librarySelection,provisioningState,csrfToken:csrf.token(req),siteName:runtimeSettings.siteName(),message,error:req.query.error||restored.error||null,welcome:req.query.welcome==='1',deliveryType:delivery,hasJellyfin,hasStremio,permanentAccess:isPermanent,stremioInstallUrl:links.installUrl,stremioManifestUrl:links.manifestUrl});}catch(error){return next(error);}});r.post('/account/provisioning/retry',requireCustomer,async(req,res)=>{if(!csrf.verify(req))return res.redirect('/account?error='+encodeURIComponent('Invalid or expired security token'));try{const outcome=await provisioning.reconcileCustomer(req.session.customerId);if(outcome?.active&&outcome?.account?.id)return res.redirect('/account?welcome=1&message='+encodeURIComponent('Your Jellyfin access has been refreshed.'));const state=await provisioning.control.getCustomerState(req.session.customerId).catch(()=>null),safe=customerProvisioningMessage(state)||'Your Jellyfin access has not completed yet. We will keep retrying automatically.';return res.redirect('/account?welcome=1&error='+encodeURIComponent(safe));}catch(error){const safe=customerProvisioningMessage({status:'failed',last_error:error?.message||error})||'Your Jellyfin access has not completed yet. We will keep retrying automatically.';return res.redirect('/account?welcome=1&error='+encodeURIComponent(safe));}});return r;}
module.exports={createCustomerDashboardRouter,hideInternalAccounts,deliveryType,sellablePlans,onboardingMessage,customerProvisioningMessage,stremioDeepLink,stremioLinks};