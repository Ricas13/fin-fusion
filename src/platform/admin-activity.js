'use strict';

const express=require('express');
const {query}=require('../db');
const activity=require('../jellyfin/activity');
const streamPolicy=require('../jellyfin/stream-policy-settings');
const runtimeSettings=require('./runtime-settings');
const csrf=require('../auth/csrf');
function requireAdminSession(req,res,next){if(req.session?.authUserId&&req.session?.authRole==='admin')return next();return res.redirect('/login?session=expired')}
function noStore(_req,res,next){res.setHeader('Cache-Control','no-store, private');res.setHeader('Pragma','no-cache');next()}
async function dashboardData(cfg){const[summaryResult,streamsResult,serversResult,eventsResult,historyResult]=await Promise.all([query(`SELECT (SELECT COUNT(*)::int FROM active_playback_sessions) AS active_streams,(SELECT COUNT(*)::int FROM active_playback_sessions WHERE playback_method='transcode') AS transcodes,(SELECT COUNT(*)::int FROM stream_policy_events WHERE created_at>=NOW()-INTERVAL '24 hours' AND decision IN('would_stop','stopped')) AS violations_24h,(SELECT COUNT(*)::int FROM stream_policy_events WHERE created_at>=NOW()-INTERVAL '24 hours' AND decision='skipped_safety') AS safety_skips_24h,(SELECT COUNT(*)::int FROM jellyfin_servers WHERE enabled=TRUE) AS enabled_servers,(SELECT COUNT(*)::int FROM jellyfin_servers WHERE enabled=TRUE AND health_status='offline') AS offline_servers,(SELECT COUNT(*)::int FROM subscriptions WHERE superseded_by IS NULL AND starts_at<=NOW() AND ((status IN('active','trialing','past_due','paused') AND current_period_end>NOW()) OR (service_extension_days>0 AND current_period_end+(service_extension_days||' days')::interval>NOW()))) AS active_subscriptions`),query(`SELECT aps.server_id,aps.jellyfin_session_id,aps.customer_id,aps.item_name,aps.item_type,aps.client_name,aps.device_name,aps.playback_method,aps.is_paused,aps.first_seen_at,aps.last_seen_at,aps.stream_limit,js.name AS server_name,ja.jellyfin_username,COALESCE(NULLIF(c.display_name,''),au.username,ja.jellyfin_username,'Customer') AS customer_name FROM active_playback_sessions aps JOIN jellyfin_servers js ON js.id=aps.server_id LEFT JOIN jellyfin_accounts ja ON ja.id=aps.jellyfin_account_id LEFT JOIN customers c ON c.id=aps.customer_id LEFT JOIN app_users au ON au.id=c.user_id ORDER BY aps.first_seen_at ASC LIMIT 250`),query(`SELECT js.id,js.name,js.server_class,js.enabled,js.max_users,js.health_status,js.last_health_check,js.placement_mode,COUNT(DISTINCT ja.id)::int AS assigned_users,COUNT(DISTINCT aps.jellyfin_session_id)::int AS active_streams FROM jellyfin_servers js LEFT JOIN jellyfin_accounts ja ON ja.server_id=js.id AND ja.disabled=FALSE LEFT JOIN active_playback_sessions aps ON aps.server_id=js.id GROUP BY js.id ORDER BY js.priority,js.name`),query(`SELECT spe.decision,spe.mode,spe.stream_count,spe.stream_limit,spe.reason,spe.created_at,js.name AS server_name,COALESCE(NULLIF(c.display_name,''),au.username,ja.jellyfin_username,'Customer') AS customer_name FROM stream_policy_events spe LEFT JOIN jellyfin_servers js ON js.id=spe.server_id LEFT JOIN jellyfin_accounts ja ON ja.id=spe.jellyfin_account_id LEFT JOIN customers c ON c.id=spe.customer_id LEFT JOIN app_users au ON au.id=c.user_id ORDER BY spe.created_at DESC LIMIT 60`),query(`SELECT ph.item_name,ph.item_type,ph.client_name,ph.device_name,ph.playback_method,ph.started_at,ph.ended_at,ph.ended_reason,js.name AS server_name,COALESCE(NULLIF(c.display_name,''),au.username,ja.jellyfin_username,'Customer') AS customer_name FROM playback_history ph JOIN jellyfin_servers js ON js.id=ph.server_id LEFT JOIN jellyfin_accounts ja ON ja.id=ph.jellyfin_account_id LEFT JOIN customers c ON c.id=ph.customer_id LEFT JOIN app_users au ON au.id=c.user_id ORDER BY ph.started_at DESC LIMIT 60`)]);const streams=streamsResult.rows,counts=new Map();for(const stream of streams){if(!stream.customer_id)continue;if(!cfg.countPaused&&stream.is_paused)continue;counts.set(stream.customer_id,(counts.get(stream.customer_id)||0)+1)}for(const stream of streams)stream.customer_stream_count=counts.get(stream.customer_id)||0;return{summary:summaryResult.rows[0],streams,servers:serversResult.rows,events:eventsResult.rows,history:historyResult.rows}}
function createAdminActivityRouter(){const router=express.Router();router.use('/admin/activity',requireAdminSession,noStore);router.get('/admin/activity',async(req,res,next)=>{try{await runtimeSettings.ensureLoaded();const policy=await streamPolicy.get(),cfg={...activity.config(),countPaused:policy.countPaused},data=await dashboardData(cfg);await query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.activity.view','admin_dashboard','activity',$2::jsonb)`,[req.session.authUserId,JSON.stringify({mode:policy.mode})]);return res.render('admin/activity',{siteName:runtimeSettings.siteName(),cfg,policy,csrfToken:csrf.token(req),message:req.query.message||null,error:req.query.error||null,...data})}catch(error){return next(error)}});router.post('/admin/activity/policy',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{await streamPolicy.save(req.body,req.session.authUserId);return res.redirect('/admin/activity?message='+encodeURIComponent('Stream policy saved. The activity worker will pick it up automatically.'))}catch(error){return res.redirect('/admin/activity?error='+encodeURIComponent(error.message))}});return router}
module.exports={createAdminActivityRouter,requireAdminSession,dashboardData};
