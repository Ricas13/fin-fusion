'use strict';

const express=require('express');
const {query,transaction}=require('../db');
const csrf=require('../auth/csrf');
const runtimeSettings=require('./runtime-settings');
const verifier=require('../payments/provider-mapping-verification');
const planPricing=require('../payments/plan-pricing');
const {esc,layout}=require('./admin-html');
const {planById,planSubnav}=require('./admin-plans');

function gate(req,res,next){if(req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId)return next();return res.redirect('/login?session=expired')}
function noStore(_req,res,next){res.setHeader('Cache-Control','no-store, private, max-age=0');res.setHeader('Pragma','no-cache');next()}
function csrfInput(req){return `<input type="hidden" name="_csrf" value="${esc(csrf.token(req))}">`}
function checked(v){return v==='on'||v==='1'||v===true}
function notice(req){return `${req.query.message?`<div class="notice success">${esc(req.query.message)}</div>`:''}${req.query.error?`<div class="notice error">${esc(req.query.error)}</div>`:''}`}
function cents(value,currency){const n=Number(value);if(!Number.isFinite(n)||n<0||n>1000000)throw new Error(`Enter a valid ${currency} price.`);return Math.round(n*100)}
function cleanExternal(value){return String(value||'').trim().slice(0,200)}
async function mappings(planId){const [prices,mapped]=await Promise.all([query(`SELECT * FROM plan_prices WHERE plan_id=$1 ORDER BY is_default DESC,currency`,[planId]),query(`SELECT pp.*,pr.currency,pr.price_minor,pr.active price_active,pr.is_default FROM plan_provider_prices pp JOIN plan_prices pr ON pr.id=pp.plan_price_id WHERE pp.plan_id=$1 ORDER BY pr.currency,pp.provider,CASE pp.checkout_mode WHEN 'payment' THEN 0 ELSE 1 END`,[planId])]);const map=new Map(mapped.rows.map(row=>[`${row.currency}:${row.provider}:${row.checkout_mode}`,row]));return{prices:prices.rows,map}}
function verification(row){if(!row)return'';const status=row.verification_status||'unverified',kind=status==='verified'||status==='not_required'?'good':status==='drift'||status==='error'?'bad':'warn';return `<div class="securityNote standalone"><span class="pill ${kind}">${esc(status)}</span> ${row.verified_at?`Verified ${esc(new Date(row.verified_at).toLocaleString('en-GB'))}`:'Not verified yet'}${row.remote_amount_minor!=null?` · provider ${esc(row.remote_currency||'')} ${(Number(row.remote_amount_minor)/100).toFixed(2)}`:''}${row.remote_interval?` / ${esc(row.remote_interval)}`:''}${row.verification_error?`<div class="subText">${esc(row.verification_error)}</div>`:''}</div>`}
function modeCard(currency,provider,mode,row,{externalRequired=false,label,help}){const key=`${currency}_${provider}_${mode}`,enabled=Boolean(row?.active),mappingField=externalRequired?`<div class="formGroup"><label>${provider==='stripe'?'Stripe Price ID':'PayPal Billing Plan ID'}</label><input class="input" name="${key}_external_id" value="${esc(row?.external_id||'')}" placeholder="${provider==='stripe'?'price_...':'P-...'}"><div class="inlineHelp">Verified against the ${currency} amount, recurrence and active state before becoming sellable.</div></div>`:`<div class="securityNote standalone">One-time PayPal orders use the CAPTAiNFiN ${currency} amount directly; no Billing Plan ID is needed.</div>`;return `<div class="serverCard"><div class="serverTop"><div><strong>${esc(label)}</strong><div class="subText">${esc(help)}</div></div><span class="pill ${enabled?'good':''}">${enabled?'Enabled':'Off'}</span></div><label class="toggleRow"><input type="checkbox" name="${key}_enabled" ${enabled?'checked':''}><span>Offer this ${currency} option</span></label>${mappingField}${verification(row)}</div>`}
function priceCard(currency,row){const enabled=Boolean(row?.active),minor=Number(row?.price_minor||0);return `<div class="serverCard"><div class="serverTop"><div><strong>${esc(currency)} price</strong><div class="subText">One currency variant of this same logical plan.</div></div>${row?.is_default?'<span class="pill accent">Default</span>':''}</div><label class="toggleRow"><input type="checkbox" name="price_${currency}_enabled" ${enabled?'checked':''}><span>Enable ${currency}</span></label><div class="formGroup"><label>Amount</label><input class="input" type="number" step="0.01" min="0" name="price_${currency}" value="${(minor/100).toFixed(2)}"></div><label class="checkRow"><input type="radio" name="default_currency" value="${currency}" ${row?.is_default?'checked':''}> Platform/default price for this plan</label></div>`}
async function page(req,plan){await runtimeSettings.ensureLoaded();const state=await mappings(plan.id),byCurrency=new Map(state.prices.map(row=>[row.currency,row])),activePrices=state.prices.filter(row=>row.active);const pricing=`<section class="section"><div class="sectionHead"><div><h2>Multi-currency pricing</h2><div class="muted">Keep one logical plan and define the enabled GBP/USD/EUR sell prices here. Access rules, duration, capacity and Jellyfin policy remain shared.</div></div></div><form class="formPanel" method="post" action="/admin/plans/${esc(plan.id)}/currency-prices">${csrfInput(req)}<div class="serverGrid">${planPricing.CURRENCIES.map(currency=>priceCard(currency,byCurrency.get(currency))).join('')}</div>${plan.audience!=='direct'?`<div class="formGrid" style="margin-top:18px"><div class="formGroup"><label>Reseller credit cost</label><input class="input" type="number" min="0" name="resellerCreditCost" value="${esc(plan.reseller_credit_cost??'')}"></div><div class="formGroup"><label>Reseller trial credit cost</label><input class="input" type="number" min="0" max="20" name="resellerTrialCreditCost" value="${esc(plan.reseller_trial_credit_cost??'')}"></div></div>`:''}<div class="securityNote standalone">Changing a price invalidates that currency variant's provider verification. Re-verify its Stripe/PayPal mappings below before it is sold again.</div><button class="button">Save currency prices</button></form></section>`;
const options=activePrices.map(price=>{const c=price.currency;return `<section class="section"><div class="sectionHead"><div><h2>${esc(c)} payment mappings</h2><div class="muted">${esc(c)} ${(Number(price.price_minor)/100).toFixed(2)} · each provider mapping belongs specifically to this currency variant.</div></div>${price.is_default?'<span class="pill accent">Default price</span>':''}</div><form class="formPanel" method="post" action="/admin/plans/${esc(plan.id)}/payment-options">${csrfInput(req)}<input type="hidden" name="currency" value="${esc(c)}"><h3>Stripe</h3><div class="serverGrid">${modeCard(c,'stripe','payment',state.map.get(`${c}:stripe:payment`),{externalRequired:true,label:'One-time payment',help:'Pay once for this plan duration; no automatic renewal.'})}${modeCard(c,'stripe','subscription',state.map.get(`${c}:stripe:subscription`),{externalRequired:true,label:'Recurring subscription',help:'Automatic Stripe renewal until cancelled.'})}</div><h3 style="margin-top:22px">PayPal</h3><div class="serverGrid">${modeCard(c,'paypal','payment',state.map.get(`${c}:paypal:payment`),{externalRequired:false,label:'One-time payment',help:'Pay once using a PayPal order.'})}${modeCard(c,'paypal','subscription',state.map.get(`${c}:paypal:subscription`),{externalRequired:true,label:'Recurring subscription',help:'Automatic renewal using a PayPal Billing Plan.'})}</div><div class="buttonRow" style="margin-top:18px"><button class="button">Verify & save ${esc(c)} mappings</button></div></form></section>`}).join('');return layout({siteName:runtimeSettings.siteName(),active:'plans',title:plan.name,subtitle:'Commerce · one plan, multiple currencies',body:`${notice(req)}${planSubnav(plan.id,'commerce')}${pricing}${options||'<div class="empty">Enable at least one price currency to configure provider mappings.</div>'}<div class="securityNote standalone"><strong>Contract safety:</strong> checkout snapshots the selected price, currency and provider mapping. Editing this catalogue later cannot rewrite an existing customer's terms.</div>`})}
async function verifyOption(plan,price,provider,mode,enabled,externalId){if(!enabled)return null;const result=await verifier.verify(provider,externalId,{priceMinor:price.price_minor,currency:price.currency,checkoutMode:mode==='payment'?'one_time':'subscription',billingInterval:plan.billing_interval});if(result.issues.length)throw new Error(`${provider==='stripe'?'Stripe':'PayPal'} ${mode==='payment'?'one-time':'subscription'} mapping does not match ${price.currency} ${(Number(price.price_minor)/100).toFixed(2)}: ${result.issues.join('; ')}`);return verifier.fields(result)}
async function saveOption(client,plan,price,provider,mode,enabled,externalId,verification=null){if(!enabled){await client.query('DELETE FROM plan_provider_prices WHERE plan_price_id=$1 AND provider=$2 AND checkout_mode=$3',[price.id,provider,mode]);return}if((provider==='stripe'||mode==='subscription')&&!externalId)throw new Error(`${provider==='stripe'?'Stripe':'PayPal'} ${mode==='payment'?'one-time':'subscription'} ID is required.`);const v=verification||{verificationStatus:'not_required',verifiedAt:new Date(),verificationError:null,remoteAmountMinor:price.price_minor,remoteCurrency:price.currency,remoteInterval:null,remoteActive:true};await client.query(`INSERT INTO plan_provider_prices(plan_id,plan_price_id,provider,external_id,checkout_mode,active,verified_at,verification_status,verification_error,remote_amount_minor,remote_currency,remote_interval,remote_active) VALUES($1,$2,$3,$4,$5,TRUE,$6,$7,$8,$9,$10,$11,$12) ON CONFLICT(plan_price_id,provider,checkout_mode) DO UPDATE SET plan_id=EXCLUDED.plan_id,external_id=EXCLUDED.external_id,active=TRUE,verified_at=EXCLUDED.verified_at,verification_status=EXCLUDED.verification_status,verification_error=EXCLUDED.verification_error,remote_amount_minor=EXCLUDED.remote_amount_minor,remote_currency=EXCLUDED.remote_currency,remote_interval=EXCLUDED.remote_interval,remote_active=EXCLUDED.remote_active,updated_at=NOW()`,[plan.id,price.id,provider,externalId||null,mode,v.verifiedAt,v.verificationStatus,v.verificationError,v.remoteAmountMinor,v.remoteCurrency,v.remoteInterval,v.remoteActive])}
function createAdminPlanPaymentOptionsRouter(){const router=express.Router();router.use('/admin/plans',gate,noStore);
router.get('/admin/plans/:id/commerce',async(req,res,next)=>{try{const plan=await planById(req.params.id);if(!plan)return res.status(404).send('Plan not found');return res.send(await page(req,plan))}catch(error){return next(error)}});
router.post('/admin/plans/:id/currency-prices',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const plan=await planById(req.params.id);if(!plan)throw new Error('Plan not found');const before=await planPricing.allPrices(plan.id),beforeMap=new Map(before.map(x=>[x.currency,x])),specs=planPricing.CURRENCIES.map(currency=>({currency,enabled:checked(req.body[`price_${currency}_enabled`]),priceMinor:cents(req.body[`price_${currency}`]||0,currency),isDefault:String(req.body.default_currency||'')===currency}));const enabled=specs.filter(x=>x.enabled);if(!enabled.length)throw new Error('At least one price currency must be enabled.');if(enabled.filter(x=>x.isDefault).length!==1)throw new Error('Choose exactly one enabled default currency.');if(specs.some(x=>x.isDefault&&!x.enabled))throw new Error('The default currency must be enabled.');await transaction(async client=>{const defaultSpec=enabled.find(x=>x.isDefault),ordered=[defaultSpec,...enabled.filter(x=>x!==defaultSpec)];for(const spec of ordered){const prior=beforeMap.get(spec.currency),changed=prior&&Number(prior.price_minor)!==spec.priceMinor;const row=await planPricing.setPrice(client,plan.id,{currency:spec.currency,priceMinor:spec.priceMinor,active:true,isDefault:spec.isDefault});if(changed)await client.query(`UPDATE plan_provider_prices SET active=FALSE,verification_status='unverified',verification_error='Plan price changed; re-verification required.',updated_at=NOW() WHERE plan_price_id=$1`,[row.id]);}for(const spec of specs.filter(x=>!x.enabled)){const prior=beforeMap.get(spec.currency);if(prior){await client.query(`UPDATE plan_prices SET active=FALSE,is_default=FALSE,updated_at=NOW() WHERE id=$1`,[prior.id]);await client.query(`UPDATE plan_provider_prices SET active=FALSE,updated_at=NOW() WHERE plan_price_id=$1`,[prior.id]);}}if(plan.audience!=='direct')await client.query(`UPDATE plans SET reseller_credit_cost=$2,reseller_trial_credit_cost=$3,updated_at=NOW() WHERE id=$1`,[plan.id,Math.max(0,parseInt(req.body.resellerCreditCost||0,10)||0),Math.max(0,Math.min(20,parseInt(req.body.resellerTrialCreditCost||0,10)||0))]);await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.plan.multicurrency.update','plan',$2,$3::jsonb)`,[req.session.authUserId,plan.id,JSON.stringify({prices:specs.map(x=>({currency:x.currency,priceMinor:x.priceMinor,enabled:x.enabled,isDefault:x.isDefault}))})])});return res.redirect(`/admin/plans/${encodeURIComponent(plan.id)}/commerce?message=`+encodeURIComponent('Currency prices saved. Re-verify any mapping whose amount changed.'))}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/commerce?error=`+encodeURIComponent(error.message||'Pricing could not be saved.'))}});
router.post('/admin/plans/:id/payment-options',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const plan=await planById(req.params.id);if(!plan)throw new Error('Plan not found');const currency=String(req.body.currency||'').toUpperCase();if(!planPricing.CURRENCIES.includes(currency))throw new Error('Unknown price currency.');const price=await planPricing.resolvePrice(plan.id,currency,{allowFallback:false});if(!price)throw new Error(`${currency} is not an enabled price for this plan.`);const specs=[['stripe','payment'],['stripe','subscription'],['paypal','payment'],['paypal','subscription']],validated=[];for(const[provider,mode]of specs){const key=`${currency}_${provider}_${mode}`,enabled=checked(req.body[`${key}_enabled`]),externalId=cleanExternal(req.body[`${key}_external_id`]);validated.push({provider,mode,enabled,externalId,verification:await verifyOption(plan,price,provider,mode,enabled,externalId)})}await transaction(async client=>{for(const spec of validated)await saveOption(client,plan,price,spec.provider,spec.mode,spec.enabled,spec.externalId,spec.verification);await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.plan.payment_options','plan',$2,$3::jsonb)`,[req.session.authUserId,plan.id,JSON.stringify({currency,verified:true})])});return res.redirect(`/admin/plans/${encodeURIComponent(plan.id)}/commerce?message=`+encodeURIComponent(`${currency} payment mappings verified and saved.`))}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/commerce?error=`+encodeURIComponent(error.message||'Payment choices could not be saved.'))}});
router.post('/admin/plans/:id/provider',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const plan=await planById(req.params.id);if(!plan)throw new Error('Plan not found');const price=await planPricing.resolvePrice(plan.id,plan.currency,{allowFallback:true});if(!price)throw new Error('Plan has no enabled default price.');const provider=['stripe','paypal'].includes(req.body.provider)?req.body.provider:null;if(!provider)throw new Error('Unknown payment provider');const mode=req.body.checkoutMode==='subscription'?'subscription':'payment',externalId=cleanExternal(req.body.externalId),enabled=Boolean(externalId)||(provider==='paypal'&&mode==='payment'),verification=await verifyOption(plan,price,provider,mode,enabled,externalId);await transaction(async client=>{await saveOption(client,plan,price,provider,mode,enabled,externalId,verification);await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.plan.provider_mapping_compat','plan',$2,$3::jsonb)`,[req.session.authUserId,plan.id,JSON.stringify({currency:price.currency,provider,mode,configured:enabled,verified:enabled})])});return res.redirect(`/admin/plans/${encodeURIComponent(plan.id)}/commerce?message=`+encodeURIComponent('Default-currency payment mapping verified and updated.'))}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/commerce?error=`+encodeURIComponent(error.message||'Payment mapping could not be updated.'))}});return router}
module.exports={createAdminPlanPaymentOptionsRouter,mappings,verifyOption,saveOption,priceCard};
