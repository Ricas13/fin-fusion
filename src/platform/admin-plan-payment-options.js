'use strict';

const express=require('express');
const {query,transaction}=require('../db');
const csrf=require('../auth/csrf');
const runtimeSettings=require('./runtime-settings');
const verifier=require('../payments/provider-mapping-verification');
const {esc,layout}=require('./admin-html');
const {planById,planSubnav}=require('./admin-plans');

function gate(req,res,next){if(req.session?.authUserId&&req.session?.authRole==='admin'&&req.session?.adminId)return next();return res.redirect('/login?session=expired')}
function noStore(_req,res,next){res.setHeader('Cache-Control','no-store, private, max-age=0');res.setHeader('Pragma','no-cache');next()}
function csrfInput(req){return `<input type="hidden" name="_csrf" value="${esc(csrf.token(req))}">`}
function checked(v){return v==='on'||v==='1'||v===true}
function notice(req){return `${req.query.message?`<div class="notice success">${esc(req.query.message)}</div>`:''}${req.query.error?`<div class="notice error">${esc(req.query.error)}</div>`:''}`}
async function mappings(planId){const result=await query(`SELECT provider,checkout_mode,external_id,active,verified_at,verification_status,verification_error,remote_amount_minor,remote_currency,remote_interval,remote_active FROM plan_provider_prices WHERE plan_id=$1 ORDER BY provider,CASE checkout_mode WHEN 'payment' THEN 0 ELSE 1 END`,[planId]);return new Map(result.rows.map(row=>[`${row.provider}:${row.checkout_mode}`,row]))}
function verification(row){if(!row)return'';const status=row.verification_status||'unverified',kind=status==='verified'||status==='not_required'?'good':status==='drift'||status==='error'?'bad':'warn';return `<div class="securityNote standalone"><span class="pill ${kind}">${esc(status)}</span> ${row.verified_at?`Verified ${esc(new Date(row.verified_at).toLocaleString('en-GB'))}`:'Not verified yet'}${row.remote_amount_minor!=null?` · provider ${esc(row.remote_currency||'')} ${(Number(row.remote_amount_minor)/100).toFixed(2)}`:''}${row.remote_interval?` / ${esc(row.remote_interval)}`:''}${row.verification_error?`<div class="subText">${esc(row.verification_error)}</div>`:''}</div>`}
function modeCard(provider,mode,row,{externalRequired=false,label,help}){const key=`${provider}_${mode}`;return `<div class="serverCard"><div class="serverTop"><div><strong>${esc(label)}</strong><div class="subText">${esc(help)}</div></div><span class="pill ${row?'good':''}">${row?'Enabled':'Off'}</span></div><label class="toggleRow"><input type="checkbox" name="${key}_enabled" ${row?'checked':''}><span>Offer this option to customers</span></label>${externalRequired?`<div class="formGroup"><label>${provider==='stripe'?'Stripe Price ID':'PayPal Billing Plan ID'}</label><input class="input" name="${key}_external_id" value="${esc(row?.external_id||'')}" placeholder="${provider==='stripe'?'price_...':'P-...'}"><div class="inlineHelp">CAPTaINFiN verifies active state, amount, currency and recurrence before saving.</div></div>`:'<div class="securityNote standalone">The one-time PayPal amount comes from this CAPTaINFiN plan price, so no separate PayPal Billing Plan ID is required.</div>'}${verification(row)}</div>`}
async function page(req,plan){await runtimeSettings.ensureLoaded();const map=await mappings(plan.id),stripePayment=map.get('stripe:payment'),stripeSubscription=map.get('stripe:subscription'),paypalPayment=map.get('paypal:payment'),paypalSubscription=map.get('paypal:subscription');const pricing=`<section class="section"><div class="sectionHead"><div><h2>Pricing</h2><div class="muted">Existing customer contracts keep their snapshotted terms. Changing this price affects new sales and can create provider mapping drift.</div></div></div><form class="formPanel" method="post" action="/admin/plans/${esc(plan.id)}/commerce">${csrfInput(req)}<div class="formGrid"><div class="formGroup"><label>Price</label><input class="input" type="number" step="0.01" min="0" name="price" value="${(Number(plan.price_minor)/100).toFixed(2)}"></div><div class="formGroup"><label>Currency</label><input class="input" name="currency" maxlength="3" value="${esc(plan.currency)}"></div></div>${plan.audience!=='direct'?`<div class="formGrid"><div class="formGroup"><label>Reseller credit cost</label><input class="input" type="number" min="0" name="resellerCreditCost" value="${esc(plan.reseller_credit_cost??'')}"></div><div class="formGroup"><label>Reseller trial credit cost</label><input class="input" type="number" min="0" max="20" name="resellerTrialCreditCost" value="${esc(plan.reseller_trial_credit_cost??'')}"></div></div>`:''}<button class="button">Save pricing</button></form></section>`;const options=`<section class="section"><div class="sectionHead"><div><h2>Customer payment choices</h2><div class="muted">Mappings are verified against the provider before they become active.</div></div></div><form class="formPanel" method="post" action="/admin/plans/${esc(plan.id)}/payment-options">${csrfInput(req)}<h3>Stripe</h3><div class="serverGrid">${modeCard('stripe','payment',stripePayment,{externalRequired:true,label:'One-time payment',help:'Pay once for this plan duration; no automatic renewal.'})}${modeCard('stripe','subscription',stripeSubscription,{externalRequired:true,label:'Recurring subscription',help:'Automatic Stripe renewal until cancelled.'})}</div><h3 style="margin-top:22px">PayPal</h3><div class="serverGrid">${modeCard('paypal','payment',paypalPayment,{externalRequired:false,label:'One-time payment',help:'Pay once using a PayPal order for this CAPTaINFiN plan price.'})}${modeCard('paypal','subscription',paypalSubscription,{externalRequired:true,label:'Recurring subscription',help:'Automatic renewal using a PayPal Billing Plan.'})}</div><div class="buttonRow" style="margin-top:18px"><button class="button">Verify & save payment choices</button></div></form></section>`;return layout({siteName:runtimeSettings.siteName(),active:'plans',title:plan.name,subtitle:'Commerce',body:`${notice(req)}${planSubnav(plan.id,'commerce')}${pricing}${options}<div class="securityNote standalone"><strong>Safety:</strong> a provider mapping whose amount, currency, active state or recurring interval disagrees with this plan is rejected instead of becoming sellable.</div>`})}
function cleanExternal(value){return String(value||'').trim().slice(0,200)}
async function verifyOption(plan,provider,mode,enabled,externalId){if(!enabled)return null;const result=await verifier.verify(provider,externalId,{priceMinor:plan.price_minor,currency:plan.currency,checkoutMode:mode==='payment'?'one_time':'subscription',billingInterval:plan.billing_interval});if(result.issues.length)throw new Error(`${provider==='stripe'?'Stripe':'PayPal'} ${mode==='payment'?'one-time':'subscription'} mapping does not match this plan: ${result.issues.join('; ')}`);return verifier.fields(result)}
async function saveOption(client,planId,provider,mode,enabled,externalId,verification=null){if(!enabled){await client.query('DELETE FROM plan_provider_prices WHERE plan_id=$1 AND provider=$2 AND checkout_mode=$3',[planId,provider,mode]);return}if((provider==='stripe'||mode==='subscription')&&!externalId)throw new Error(`${provider==='stripe'?'Stripe':'PayPal'} ${mode==='payment'?'one-time':'subscription'} ID is required.`);const v=verification||{verificationStatus:'not_required',verifiedAt:new Date(),verificationError:null,remoteAmountMinor:null,remoteCurrency:null,remoteInterval:null,remoteActive:true};await client.query(`INSERT INTO plan_provider_prices(plan_id,provider,external_id,checkout_mode,active,verified_at,verification_status,verification_error,remote_amount_minor,remote_currency,remote_interval,remote_active) VALUES($1,$2,$3,$4,TRUE,$5,$6,$7,$8,$9,$10,$11) ON CONFLICT(plan_id,provider,checkout_mode) DO UPDATE SET external_id=EXCLUDED.external_id,active=TRUE,verified_at=EXCLUDED.verified_at,verification_status=EXCLUDED.verification_status,verification_error=EXCLUDED.verification_error,remote_amount_minor=EXCLUDED.remote_amount_minor,remote_currency=EXCLUDED.remote_currency,remote_interval=EXCLUDED.remote_interval,remote_active=EXCLUDED.remote_active,updated_at=NOW()`,[planId,provider,externalId||null,mode,v.verifiedAt,v.verificationStatus,v.verificationError,v.remoteAmountMinor,v.remoteCurrency,v.remoteInterval,v.remoteActive])}
function createAdminPlanPaymentOptionsRouter(){const router=express.Router();router.use('/admin/plans',gate,noStore);router.get('/admin/plans/:id/commerce',async(req,res,next)=>{try{const plan=await planById(req.params.id);if(!plan)return res.status(404).send('Plan not found');return res.send(await page(req,plan))}catch(error){return next(error)}});router.post('/admin/plans/:id/payment-options',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const plan=await planById(req.params.id);if(!plan)throw new Error('Plan not found');const specs=[['stripe','payment'],['stripe','subscription'],['paypal','payment'],['paypal','subscription']],validated=[];for(const[provider,mode]of specs){const key=`${provider}_${mode}`,enabled=checked(req.body[`${key}_enabled`]),externalId=cleanExternal(req.body[`${key}_external_id`]);validated.push({provider,mode,enabled,externalId,verification:await verifyOption(plan,provider,mode,enabled,externalId)})}await transaction(async client=>{for(const spec of validated)await saveOption(client,plan.id,spec.provider,spec.mode,spec.enabled,spec.externalId,spec.verification);await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.plan.payment_options','plan',$2,$3::jsonb)`,[req.session.authUserId,plan.id,JSON.stringify({verified:true,stripe:{payment:checked(req.body.stripe_payment_enabled),subscription:checked(req.body.stripe_subscription_enabled)},paypal:{payment:checked(req.body.paypal_payment_enabled),subscription:checked(req.body.paypal_subscription_enabled)}})])});return res.redirect(`/admin/plans/${encodeURIComponent(plan.id)}/commerce?message=`+encodeURIComponent('Payment choices verified and saved.'))}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/commerce?error=`+encodeURIComponent(error.message||'Payment choices could not be saved.'))}});router.post('/admin/plans/:id/provider',async(req,res)=>{if(!csrf.verify(req))return res.status(403).send('Invalid security token');try{const plan=await planById(req.params.id);if(!plan)throw new Error('Plan not found');const provider=['stripe','paypal'].includes(req.body.provider)?req.body.provider:null;if(!provider)throw new Error('Unknown payment provider');const mode=req.body.checkoutMode==='subscription'?'subscription':'payment',externalId=cleanExternal(req.body.externalId),enabled=Boolean(externalId)||(provider==='paypal'&&mode==='payment'),verification=await verifyOption(plan,provider,mode,enabled,externalId);await transaction(async client=>{await saveOption(client,plan.id,provider,mode,enabled,externalId,verification);await client.query(`INSERT INTO audit_log(actor_user_id,action,entity_type,entity_id,metadata) VALUES($1,'admin.plan.provider_mapping_compat','plan',$2,$3::jsonb)`,[req.session.authUserId,plan.id,JSON.stringify({provider,mode,configured:enabled,verified:enabled})])});return res.redirect(`/admin/plans/${encodeURIComponent(plan.id)}/commerce?message=`+encodeURIComponent('Payment mapping verified and updated.'))}catch(error){return res.redirect(`/admin/plans/${encodeURIComponent(req.params.id)}/commerce?error=`+encodeURIComponent(error.message||'Payment mapping could not be updated.'))}});return router}
module.exports={createAdminPlanPaymentOptionsRouter,mappings,verifyOption};
