'use strict';

require('dotenv').config();
const path=require('path');
const {randomUUID}=require('crypto');
const express=require('express');
const session=require('express-session');
const PgStore=require('connect-pg-simple')(session);
const {query}=require('./db');
const firstRun=require('./auth/first-run-setup');
const controller=require('./auth/staff-controller');
const {guardSession}=require('./auth/session-guard');
const runtimeSettings=require('./platform/runtime-settings');
const operationsSettings=require('./platform/operations-settings');
const adminNav=require('./platform/admin-nav');
const {consumeLoginAttempt,pruneLoginRateLimits}=require('./security/login-rate-limit');
const customerRateLimit=require('./security/customer-rate-limit');
const {requestMaintenanceGuard}=require('./security/maintenance-lock');
const IS_PRODUCTION=String(process.env.NODE_ENV||'').toLowerCase()==='production',PORT=Number(process.env.PORT||3030),SESSION_SECRET=String(process.env.SESSION_SECRET||'');
function fail(message){throw new Error(`Startup configuration error: ${message}`);}
function validateEnvironment(){if(IS_PRODUCTION&&!process.env.DATABASE_URL)fail('DATABASE_URL is required in production.');if(!SESSION_SECRET||SESSION_SECRET.length<32||/change[-_ ]?(me|this)|example|placeholder/i.test(SESSION_SECRET))fail('SESSION_SECRET must be a unique random value of at least 32 characters.');if(process.env.ADMIN_PASSWORD&&String(process.env.ADMIN_PASSWORD).length<12)fail('ADMIN_PASSWORD must be at least 12 characters when supplied.');if(String(process.env.ADMIN_PASSWORD||'')==='admin123')fail('The legacy admin123 password is not permitted.');}
function requestContext(req,res,next){const requestId=randomUUID();req.requestId=requestId;res.setHeader('X-Request-Id',requestId);return next();}
function securityHeaders(req,res,next){res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('X-Frame-Options','DENY');res.setHeader('Referrer-Policy','same-origin');res.setHeader('Permissions-Policy','camera=(), microphone=(), geolocation=()');res.setHeader('Cross-Origin-Opener-Policy','same-origin');res.setHeader('Cross-Origin-Resource-Policy','same-origin');res.setHeader('Content-Security-Policy',"default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; object-src 'none'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'");if(IS_PRODUCTION)res.setHeader('Strict-Transport-Security','max-age=31536000; includeSubDomains');if(!['GET','HEAD','OPTIONS'].includes(req.method)){if(req.get('sec-fetch-site')==='cross-site')return res.status(403).send('Cross-site request blocked');const origin=req.get('origin'),host=req.get('x-forwarded-host')||req.get('host');if(origin&&host){try{if(new URL(origin).host!==host)return res.status(403).send('Origin mismatch');}catch(_){return res.status(403).send('Invalid origin');}}}return next();}
function sessionMiddleware(){const options={secret:SESSION_SECRET,name:process.env.SESSION_COOKIE_NAME||'steamfusion.sid',proxy:true,resave:false,saveUninitialized:false,cookie:{maxAge:86400000,httpOnly:true,sameSite:'lax',secure:process.env.COOKIE_SECURE?process.env.COOKIE_SECURE==='true':IS_PRODUCTION}};if(process.env.DATABASE_URL)options.store=new PgStore({conString:process.env.DATABASE_URL,createTableIfMissing:true,tableName:'user_sessions',pruneSessionInterval:900});return session(options);}
async function staffLoginRateLimit(req,res,next){if(req.method!=='POST'||req.path!=='/login')return next();try{const result=await consumeLoginAttempt(req.ip||req.socket?.remoteAddress||'unknown',{windowMs:Number(process.env.LOGIN_RATE_LIMIT_WINDOW_MS||900000),maxAttempts:Number(process.env.LOGIN_RATE_LIMIT_MAX_ATTEMPTS||10),secret:SESSION_SECRET});res.setHeader('X-RateLimit-Limit',String(result.maxAttempts));res.setHeader('X-RateLimit-Remaining',String(Math.max(0,result.maxAttempts-result.attemptCount)));if(!result.allowed){res.setHeader('Retry-After',String(Math.max(1,result.retryAfterSeconds)));return res.status(429).send('Too many login attempts. Try again later.');}return next();}catch(error){console.error('Persistent staff login limiter unavailable:',error.message);return IS_PRODUCTION?res.status(503).send('Login temporarily unavailable. Try again shortly.'):next();}}
async function customerAuthRateLimit(req,res,next){if(req.method!=='POST'||!['/account/login','/account/forgot-password'].includes(req.path))return next();const reset=req.path==='/account/forgot-password';try{const result=await customerRateLimit.consume(`${reset?'customer-reset':'customer-login'}:${req.ip||req.socket?.remoteAddress||'unknown'}`,{limit:reset?5:10,windowMs:900000});if(!result.allowed){res.setHeader('Retry-After',String(Math.max(1,Math.ceil((result.resetAt.getTime()-Date.now())/1000))));return res.status(429).send(reset?'Too many password reset requests. Try again later.':'Too many login attempts. Try again later.');}return next();}catch(error){console.error('Persistent customer auth limiter unavailable:',error.message);return IS_PRODUCTION?res.status(503).send('Authentication temporarily unavailable.'):next();}}
async function publicMutationRateLimit(req,res,next){if(req.method!=='POST')return next();const requestPath=req.path||'',kind=requestPath==='/account/register'?'registration':requestPath.startsWith('/activate/')?'activation':requestPath.includes('/claim')?'claim':null;if(!kind)return next();try{const ops=await operationsSettings.get(),limit=kind==='registration'?ops.registrationRateLimitPerHour:Math.max(10,ops.registrationRateLimitPerHour*2),result=await customerRateLimit.consume(`public-${kind}:${req.ip||req.socket?.remoteAddress||'unknown'}`,{limit,windowMs:3600000});if(!result.allowed){res.setHeader('Retry-After',String(Math.max(1,Math.ceil((result.resetAt.getTime()-Date.now())/1000))));return res.status(429).send('Too many requests from this address. Try again later.');}return next();}catch(error){console.error('Public mutation limiter unavailable:',error.message);return IS_PRODUCTION?res.status(503).send('This action is temporarily unavailable.'):next();}}
async function loginSetupGate(req,res,next){try{if(await firstRun.isSetupRequired())return res.redirect('/setup');await runtimeSettings.ensureLoaded().catch(()=>{});return next();}catch(error){return next(error);}}
function mountPlatform(app){
 const{createHealthRouter}=require('./platform/health'),{createWebhookRouter}=require('./platform/webhooks'),{createStremioRuntimeRouter}=require('./stremio/runtime');app.use(createHealthRouter());app.use(createWebhookRouter());app.use(createStremioRuntimeRouter());app.use(express.urlencoded({extended:true,limit:'1mb'}));app.use(express.json({limit:'1mb'}));app.use(express.static(path.join(__dirname,'..','public'),{maxAge:IS_PRODUCTION?'1h':0}));app.use(requestMaintenanceGuard);app.use(sessionMiddleware());app.use(guardSession);app.use(staffLoginRateLimit);app.use(customerAuthRateLimit);app.use(publicMutationRateLimit);
 const{createFirstRunRouter}=require('./auth/first-run-controller'),{createAdminSecurityRouter}=require('./platform/admin-security');app.use(createFirstRunRouter());app.get('/login',loginSetupGate,controller.loginPage);app.post('/login',loginSetupGate,controller.loginSubmit);app.get('/logout',controller.logout);app.use(controller.createAuthRouter());app.use(createAdminSecurityRouter());
 const{storefrontPage}=require('./platform/storefront');app.get('/',async(req,res,next)=>{try{if(await firstRun.isSetupRequired())return res.redirect('/setup');return storefrontPage(req,res,next);}catch(e){next(e);}});
 const{createRouter}=require('./platform/router'),{createCustomerPasswordSyncRouter}=require('./platform/customer-password-sync'),{createCustomerSubscriptionActionsRouter}=require('./platform/customer-subscription-actions'),{createFlexibleCheckoutRouter}=require('./platform/flexible-checkout'),{createCustomerClaimRouter}=require('./platform/customer-claim'),{createResellerMonthlyPortalRouter}=require('./platform/reseller-monthly-portal'),{createAdminCustomerCreateRouter}=require('./platform/admin-customer-create'),{createAdminPlanCreateV2Router}=require('./platform/admin-plan-create-v2'),{createAdminPlanLifecycleRouter}=require('./platform/admin-plan-lifecycle'),{createAdminPlanInventoryRouter}=require('./platform/admin-plan-inventory'),{createAdminPlansListRouter}=require('./platform/admin-plans-list'),{createAdminPlanLibrariesRouter}=require('./platform/admin-plan-libraries'),{createAdminPlanPlacementFleetRouter}=require('./platform/admin-plan-placement-fleet'),{createAdminPlanPlacementRouter}=require('./platform/admin-plan-placement'),{createAdminPlanPaymentOptionsRouter}=require('./platform/admin-plan-payment-options'),{createAdminPaymentSettingsRouter}=require('./platform/admin-payment-settings'),{createAdminProviderMappingsRouter}=require('./platform/admin-provider-mappings'),{createAdminBillingRouter}=require('./platform/admin-billing'),{createAdminEmailRouter}=require('./platform/admin-email'),{createAdminProvisioningRouter}=require('./platform/admin-provisioning'),{createAdminDriftRouter}=require('./platform/admin-drift'),{createAdminRequestUsersRouter}=require('./platform/admin-request-users'),{createAdminRequestPlanPolicyRouter}=require('./platform/admin-request-plan-policy'),{createAdminRequestRedirectRouter}=require('./platform/admin-request-redirect'),{createAdminServerMigrationsRouter}=require('./platform/admin-server-migrations'),{createAdminServerUsersRouter,createLegacyJellyfinImportRedirectRouter}=require('./platform/admin-server-users'),{createAdminSetupRouter}=require('./platform/admin-setup'),{createAdminConfigurationTransferRouter}=require('./platform/admin-configuration-transfer'),{createAdminLibrariesRouter}=require('./platform/admin-libraries'),{createAdminServerFleetDashboardRouter}=require('./platform/admin-server-fleet-dashboard'),{createAdminServerLibraryDashboardRouter}=require('./platform/admin-server-library-dashboard'),{createAdminOriginalSettingsRouter}=require('./platform/admin-original-settings'),{createAdminBrandingRouter}=require('./platform/admin-branding'),{createBrandingRouter}=require('./platform/branding'),{createAdminPreviewRouter}=require('./platform/admin-preview'),{createAdminActivityRouter}=require('./platform/admin-activity'),{createAdminCustomer360Router}=require('./platform/admin-customer-360'),{createAdminUsersRouter}=require('./platform/admin-users'),{createAdminServersRouter}=require('./platform/admin-servers'),{createAdminDiscountsRouter}=require('./platform/admin-discounts'),{createAdminReferralsRouter}=require('./platform/admin-referrals'),{createAdminPlansRouter}=require('./platform/admin-plans'),{createAdminJobsRouter}=require('./platform/admin-jobs'),{createAdminBulkCustomersRouter}=require('./platform/admin-bulk-customers'),{createAdminCustomersListRouter}=require('./platform/admin-customers-list'),{createAdminOperationsRouter}=require('./platform/admin-operations'),{createAdminBackupsRouter}=require('./platform/admin-backups'),{createAdminSupportPolicyRouter}=require('./platform/admin-support-policy'),{createAdminAttentionRouter}=require('./platform/admin-attention'),{createAdminCatalogVersioningRouter}=require('./platform/admin-catalog-versioning'),dashboard=require('./platform/admin-dashboard');
 app.use(createBrandingRouter());app.use(createCustomerClaimRouter());app.use(createCustomerPasswordSyncRouter());app.use(createCustomerSubscriptionActionsRouter());app.use(createFlexibleCheckoutRouter());app.use(createResellerMonthlyPortalRouter());app.use(createAdminPreviewRouter());
 app.use('/invite',(_req,res)=>res.status(410).send('Invitation onboarding is no longer available.'));
 app.use('/admin/invitations',(_req,res)=>res.redirect(302,'/admin/users?message='+encodeURIComponent('Invitations are retired. Add or import customers instead.')));
 // Legacy reseller-admin pages from the former wallet/credit product remain retired.
 // The live reseller product is the monthly seat-management portal mounted above.
 app.use(/^\/admin\/reseller(?:\/|$)/,(_req,res)=>res.redirect(302,'/admin/plans?message='+encodeURIComponent('Reseller plans are managed from Plans.')));
 app.get('/admin',dashboard.dashboardPage);app.use(createAdminAttentionRouter());app.use(createAdminSetupRouter());app.use(createAdminOperationsRouter());app.use(createAdminBackupsRouter());app.use(createAdminSupportPolicyRouter());app.use(createAdminCatalogVersioningRouter());app.use(createAdminConfigurationTransferRouter());app.use(createAdminOriginalSettingsRouter());app.use(createAdminBrandingRouter());app.use(createAdminServerUsersRouter());app.use(createLegacyJellyfinImportRedirectRouter());app.use(createAdminServerMigrationsRouter());app.use(createAdminRequestPlanPolicyRouter());app.use(createAdminRequestUsersRouter());app.use(createAdminRequestRedirectRouter());app.use(createAdminProvisioningRouter());app.use(createAdminDriftRouter());app.use(createAdminEmailRouter());app.use(createAdminPaymentSettingsRouter());app.use(createAdminProviderMappingsRouter());app.use(createAdminBillingRouter());app.use(createAdminCustomerCreateRouter());app.use(createAdminPlanCreateV2Router());app.use(createAdminPlanLifecycleRouter());app.use(createAdminPlanInventoryRouter());app.use(createAdminPlansListRouter());app.use(createAdminPlanPaymentOptionsRouter());app.use(createAdminPlansRouter());app.use(createAdminPlanPlacementFleetRouter());app.use(createAdminPlanPlacementRouter());app.use(createAdminJobsRouter());app.use(createAdminBulkCustomersRouter());app.use(createAdminCustomersListRouter());app.use(createAdminPlanLibrariesRouter());app.use(createAdminServerFleetDashboardRouter());app.use(createAdminServerLibraryDashboardRouter());app.use(createAdminServersRouter());app.use(createAdminActivityRouter());app.use(createAdminLibrariesRouter());app.use(createAdminCustomer360Router());app.use(createAdminUsersRouter());app.use(createAdminDiscountsRouter());app.use(createAdminReferralsRouter());app.use(createRouter());
}
function createApplication(){validateEnvironment();const app=express();app.set('trust proxy',1);app.disable('x-powered-by');app.set('view engine','ejs');app.set('views',path.join(__dirname,'..','views'));app.locals.adminNavGroups=adminNav.groups;app.locals.adminNavActiveKey=adminNav.activeKey;app.locals.adminNavGroupFor=adminNav.groupFor;app.locals.adminNavSidebarKey=adminNav.sidebarKey;app.use(requestContext);app.use(securityHeaders);mountPlatform(app);app.use((req,res)=>res.status(404).send('Not found'));app.use((error,req,res,_next)=>{const routeTemplate=req.route?.path?`${req.baseUrl||''}${req.route.path}`:null;console.error('Request failed:',{requestId:req.requestId||null,method:req.method,route:routeTemplate,error:error.message});if(res.headersSent)return;const status=Number(error.status||error.statusCode||500);res.status(status>=400&&status<600?status:500).send(status>=500?'Request failed.':error.message);});return app;}
async function startupSummary(){try{const servers=await query('SELECT COUNT(*)::int n FROM jellyfin_servers WHERE enabled=TRUE');console.log(Number(servers.rows[0]?.n||0)>0?`Jellyfin: ${servers.rows[0].n} enabled server(s) configured`:'Jellyfin: not configured');}catch(error){console.warn('Jellyfin startup summary unavailable:',error.message);}}
function start(){const app=createApplication(),server=app.listen(PORT,()=>{console.log('Steam Fusion running');console.log(`CAPTAiNFiN web application listening on http://127.0.0.1:${PORT}`);startupSummary().catch(()=>{});}),prune=setInterval(()=>Promise.all([pruneLoginRateLimits(),customerRateLimit.cleanup()]).catch(error=>console.warn('Rate-limit cleanup failed:',error.message)),3600000);prune.unref?.();return{app,server};}
if(require.main===module)start();module.exports={createApplication,start,securityHeaders,validateEnvironment,startupSummary};
