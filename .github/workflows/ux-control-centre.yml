name: UX Control Centre
on:
  pull_request:
    paths:
      - 'src/platform/**'
      - 'src/entitlements/**'
      - 'src/jellyfin/**'
      - 'views/customer/**'
      - 'public/css/**'
      - 'scripts/ux-control-centre-smoke.js'
      - 'scripts/ux-release-contract-smoke.js'
      - '.github/workflows/ux-control-centre.yml'
jobs:
  ux-control-centre:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/ux-control-centre-smoke.js
      - run: node scripts/ux-release-contract-smoke.js
