<%
  // adminNavGroupFor is supplied through Express app.locals. Its renderHeader
  // bridge resolves the same canonical header() used by string-rendered pages,
  // so legacy EJS screens cannot drift or import server modules themselves.
%><%- adminNavGroupFor.renderHeader(activeNav, siteName) %>
