<%
  // Legacy EJS screens render the exact same rail markup as the string-based
  // admin shell. Keeping one renderer also means both paths consume the shared
  // admin-icons module instead of carrying a second iconPaths object here.
  const adminCore = require(process.cwd() + '/src/platform/admin-html-core-base');
%><%- adminCore.header(activeNav, siteName) %>
