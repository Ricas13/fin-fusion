'use strict';

// Deprecated compatibility module. Runtime composition moved to
// src/application.js. Kept as a no-op for old external launch commands so an
// upgrade does not fail solely because NODE_OPTIONS still references it.
module.exports = {};
